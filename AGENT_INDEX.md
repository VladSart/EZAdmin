# EZAdmin — Agent Index & Master Routing

> **For AI agents:** Read this file first. It tells you where to go, how to respond, and what to check across modules before answering any question.

---

## Universal Response Format

Every response in this repo follows three layers. **Never skip any layer.**

### Layer 1 — Hotfix Mode
> Used when: someone needs this fixed NOW. Client is down, ticket is open.

Structure:
1. **Triage** — 3–5 commands to confirm the break in under 60 seconds
2. **Fix** — ordered steps, highest-probability cause first
3. **Validate** — proof commands that confirm it's actually fixed

Rules: No theory. No history. Every command has a reason. Strong "if X → do Y" language.

---

### Layer 2 — Deep Dive Mode
> Used when: understanding the system, writing a post-mortem, building something, or the hotfix didn't work.

Structure:
1. Full dependency chain (what must be true for this to work)
2. How the system actually works in this environment
3. Symptom → cause map
4. All remediation paths with rollback notes
5. Community findings (Reddit r/sysadmin, r/Intune, r/Office365, Spiceworks, MS Tech Community)
6. Evidence pack for escalation

Rules: Explain why, not just what. Include architecture. Flag policy override risks (Intune/GPO always win).

---

### Layer 3 — Learning Pointers
> Always appended after any Hotfix or Deep Dive response.

Format: 3–6 bullet points. Each one is a concept, topic, or resource tied directly to what just happened — not generic study advice. Goal: you get better every time you close a ticket.

---

## Domain Map — Where to Route

| Topic | Primary Folder | Also Check |
|-------|---------------|------------|
| DFS Namespace failures, referrals, access | `DFS/` | `EntraID/`, `Windows/` |
| DFS Replication backlog, SYSVOL, conflicts | `DFS/` | `Windows/` |
| Intune enrollment failures | `Intune/` | `Autopilot/`, `EntraID/` |
| Intune policy conflicts, compliance not applying | `Intune/` | `EntraID/`, `Windows/` |
| Endpoint analytics (Startup performance / App reliability / Work from anywhere scoring) | `Intune/` | `Autopilot/` (deployment profile assignment), `Windows/` (WUfB/feature updates) |
| Autopilot enrollment, hybrid join, hash upload | `Autopilot/` | `Intune/`, `EntraID/` |
| Windows Autopilot device preparation (APDP) — Entra-join-only enrollment mode | `Autopilot/` | `Intune/`, `EntraID/` |
| Entra ID join, PRT issues, device registration | `EntraID/` | `Intune/`, `Security/ConditionalAccess/` |
| Conditional Access policy conflicts, CA failures | `Security/ConditionalAccess/` | `EntraID/`, `Intune/` |
| Hybrid join (HAADJ), Entra Connect Sync (legacy on-prem sync-engine model) | `EntraID/` | `Security/ConditionalAccess/` |
| Entra Cloud Sync (provisioning-agent model — gMSA, multi-agent HA, disconnected forests, Group Provisioning to AD DS) | `EntraID/` | `ActiveDirectory/` (gMSA/schema prerequisites live in on-prem AD) |
| Windows Update, WfUB, WSUS conflicts | `Windows/` | `Intune/` |
| BitLocker, key escrow, recovery | `Windows/` | `Intune/`, `EntraID/` |
| NPS/RADIUS server — connection request/network policy evaluation, RADIUS client registration, VPN and 802.1X wired/wireless auth backend, NPS Extension for Entra MFA | `Windows/` | `Windows/` AlwaysOnVPN topic (client/RRAS-side VPN symptoms that hand off to NPS for user-tunnel auth), `macOS/` WiFi-8021x topic (client-side certificate trust for the same RADIUS server, explicitly out of scope there) |
| DHCP client failures (APIPA, lease/relay/scope-option symptoms on a single device) | `Windows/` (DHCP-Client) | `Windows/` (DHCP-Server, when the root cause is server-wide) |
| DHCP Server role — scope/superscope exhaustion, DHCP Failover (hot standby/load balance, MCLT split-brain safety), DHCP Policies, secure dynamic DNS update credential expiry, JET database corruption/backup/restore, audit logging | `Windows/` (DHCP-Server) | `Windows/` (DHCP-Client, for the client-side DORA/relay half of the same protocol), `ActiveDirectory/` (AD-integrated DNS zone that DHCP registers records into) |
| Hyper-V host & VM — VM state/Integration Services, checkpoint/AVHDX chain failures, virtual switch architecture, Live Migration (Kerberos/CredSSP auth, Event ID 21502 family), Failover Clustering integration (CSV, quorum, possible-owner nodes), Hyper-V Replica (HRL growth, resync) | `Windows/` (HyperV) | `ActiveDirectory/` (Kerberos Constrained Delegation for Live Migration auth, domain trust for cross-forest Replica), `Azure/AVD/` (cloud-hosted session VMs — a materially different management plane, explicitly out of scope here), `Windows/` (StorageSpacesDirect — the storage foundation a clustered Hyper-V deployment runs on, explicitly out of scope in the Hyper-V topic itself) |
| Storage Spaces Direct (S2D) — hyperconverged pool/virtual disk/physical disk health states, cache-tier vs. capacity-tier architecture, resiliency types (mirror/parity/nested), Health Service auto-repair, pool-level quorum (distinct from cluster quorum), storage-network (RDMA) dependency | `Windows/` (StorageSpacesDirect) | `Windows/` (HyperV — the CSV/cluster layer S2D runs underneath), `Azure/AVD/` (FSLogix profile storage may sit on S2D-backed volumes, a deployment detail not this topic's own scope) |
| Volume Shadow Copy Service (VSS) — requestor/writer/provider snapshot architecture, writer freeze/thaw timeout failures, shadow storage exhaustion, SQL Server VSS writer isolation (SQLWRITER/SQLVDI), "no writers listed" VSS/COM+ registration repair | `Windows/` (VSS) | `Azure/Backup/` (the backup product consuming VSS snapshots, a separate failure domain once VSS itself is confirmed healthy), `ActiveDirectory/` (AD's own System State backup depends on the same VSS mechanism) |
| Windows Server Failover Clustering (WSFC) — quorum voting mechanics, disk/file share/cloud witness types, dynamic quorum management, cluster networking (Partitioned vs. Down), node quarantine (Health Service auto-isolation), Cluster-Aware Updating | `Windows/` (FailoverClustering) | `Windows/` (HyperV and StorageSpacesDirect — both build on this cluster layer and each have their own additional, independently-gated health concept layered on top: CSV/Live Migration for Hyper-V, pool-level quorum for S2D) |
| WSUS server role — SUSDB engine (WID vs. full SQL Server) and maintenance (reindex, decline-superseded, cleanup), content/metadata consistency (wsusutil checkhealth/reset), IIS WsusPool memory-exhaustion crashes, multi-tier hierarchy maintenance ordering | `Windows/` (WSUS-Server, under Windows Update) | `Windows/` (WSUS to WfUB — the client-side scanning-source/migration topic this server-side runbook is the explicit prerequisite for) |
| WinRM / PowerShell Remoting — listener/firewall/network-profile dependency chain, Kerberos vs. NTLM auth path determination, GPO-managed WinRM configuration precedence over local `Enable-PSRemoting`, Kerberos double-hop (CredSSP vs. classic constrained delegation vs. resource-based constrained delegation), CredSSP Encryption Oracle Remediation (CVE-2018-0886), WSMan quota exhaustion under automation/CI load | `Windows/` (WinRM) | `ActiveDirectory/` (KerberosDelegation — the actual RBCD/classic-constrained-delegation configuration for the double-hop fix), `EntraID/` (unrelated — Entra-side Kerberos/PRT topics are a different auth surface) |
| Windows Backup for Organizations (Windows settings backup and restore) — settings/Microsoft-Store-app-list continuity only, explicitly NOT file/document backup; three independently-gated capabilities (backup, OOBE restore, first-sign-in restore) each with their own join-type/OS-build minimums; five unrelated-sounding companion policies (EnableActivityFeed/PublishUserActivities/UploadUserActivities/EnableCDP/AllowConnectedDevices) that silently block backup if any is Disabled; GPO+CSP mixing explicitly unsupported; Conditional Access and PRMFA interference during OOBE restore; July 2026 Enterprise State Roaming management consolidation; Windows 11 26H2 default-on-for-backup-only baseline change | `Windows/` (WindowsBackup) | `M365/SharePoint-OneDrive/` (OneDrive Known Folder Move — the actual file/document continuity mechanism, the single most common scope-confusion redirect for this topic), `Intune/` (policy deployment channel — Settings Catalog/CSP), `EntraID/` (Conditional Access exclusion required for the restore app during OOBE) |
| BranchCache — WAN caching for SMB/Web/BITS content; distributed cache mode (WS-Discovery multicast, single-subnet-only) vs. hosted cache mode (centralized, multi-subnet-capable); content-information V1/V2 cross-version incompatibility blocking cache sharing between mixed-generation clients in distributed mode; per-SMB-share hash publication (the most common "nothing caches" root cause, independent of the role service being installed); undocumented hard dependency on Offline Files (`CscService`) for SMB caching; not supported for Delivery-Optimization-sourced Windows Update content on Windows 11 | `Windows/` (BranchCache) | `Windows/` (FolderRedirection/Offline Files — shares the same `CscService` dependency), `Windows/` (DeliveryOptimization — the Windows 11 Update-traffic alternative BranchCache can no longer accelerate) |
| Folder Redirection & Offline Files (CSC) — User Configuration GPO folder relocation to a network share paired with Offline Files client-side caching; target-location modes and policy-removal (copy-back vs. leave-in-place) behavior; Sync Center conflict resolution as expected multi-writer behavior, not data loss; per-USER (not centrally GPO-enforced) offline-availability pinning state that can outlive a profile change; Always Offline mode (`Configure slow-link mode` Latency=1) vs. adaptive slow-link fallback; documented AppData\Roaming redirection anti-pattern (open-file-handle conflict risk) | `Windows/` (FolderRedirection) | `Windows/` (BranchCache — shares the same `CscService` dependency), `M365/SharePoint-OneDrive/` (OneDrive Known Folder Move — the modern Entra-native replacement for Desktop/Documents/Pictures redirection), `Windows/` (WindowsBackup — the settings-continuity alternative to AppData\Roaming redirection) |
| Power Automate flows, connectors, licensing | `PowerAutomate/` | `M365/SharePoint-OneDrive/`, `EntraID/` |
| Power Automate Desktop (RPA) — machine registration/direct connectivity (gateways retired), attended/unattended session model, UIFlowService, machine groups, Process/Unattended RPA capacity licensing | `PowerAutomate/Desktop-RPA/` | `PowerAutomate/Troubleshooting/` (cloud-flow-side ownership/connector auth — a separate failure domain from the machine runtime), `Intune/` (if PAD is deployed via Intune, not a runtime issue) |
| SharePoint site creation, permissions via automation | `PowerAutomate/SharePoint/` | `EntraID/`, `M365/SharePoint-OneDrive/` |
| Exchange Online mail flow, rules, hybrid | `M365/Exchange/` | `EntraID/` |
| Outlook desktop client (classic vs. New Outlook, Autodiscover, profile/OST, connection status, credential loops) | `M365/Exchange/` | `EntraID/` (auth/token layer), `Security/ConditionalAccess/` (legacy-auth blocks surfacing as client sign-in loops) |
| Exchange Online mailbox migration batches — Cutover (single-batch, ≤2,000/practical ≤150), Staged (legacy Exchange 2003/2007 source only), IMAP (incl. Google Workspace), Remote Move (hybrid onboarding/offboarding via `New-MoveRequest`), and Cross-tenant (tenant-to-tenant, org-relationship + `MailUser` pre-staging) migration batch mechanics, WLM/MRS/MRSProxy throttling, and Data Consistency Score skipped-item handling | `M365/Exchange/` (`MigrationBatches-A/B.md`, `Scripts/Get-MigrationBatchHealth.ps1`) | `M365/Exchange/Hybrid-Coexistence-A.md` (hybrid topology/HCW/routing — the prerequisite this topic assumes is already in place), `EntraID/Troubleshooting/CrossTenant-A.md` (Entra B2B/external-identity layer — a different configuration surface from the Exchange organization-relationship mailbox-move mechanics here) |
| SharePoint/OneDrive sync, permissions, migration | `M365/SharePoint-OneDrive/` | `PowerAutomate/` |
| Teams calling, policies, devices | `M365/Teams/` | `EntraID/` |
| M365 licensing, group-based, service plans | `M365/Licensing/` | `EntraID/` |
| Defender for Endpoint onboarding, ASR, alerts | `Security/Defender/` | `Intune/`, `EntraID/` |
| Defender for Endpoint on macOS — the 12-component Intune deployment (System Extensions/Network Filter/Full Disk Access/Background Services/Notifications/Accessibility/Bluetooth/AutoUpdate profiles, app, and a separately-licensed onboarding package), `mdatp` CLI health, single-system-wide-Network-Filter-slot conflicts with other AV/EDR, SSL-inspecting-proxy incompatibility | `macOS/` | `Security/Defender/MDE-Onboarding-A.md` (Windows-only equivalent — SENSE service/registry, explicitly out of scope for macOS), `macOS/Troubleshooting/Extensions-A.md` (vendor-agnostic system extension/PPPC mechanics this topic builds on top of) |
| Apple Device Migration — macOS 26+ Apple Business/School Manager wipe-free MDM-to-MDM re-enrollment ("Assign Device Management", new-MDM-profile-installs-before-old-one-removed cutover, Apple-enforced OS/ownership/enrollment eligibility gate invisible to Intune) and Managed Migration Assistant (macOS 26.4+ destination, declarative `RequiredPaths`/`ExcludedPaths` control over Mac-to-Mac Home-folder data transfer during Setup Assistant); neither mechanism copies configuration profiles/policies/scripts from the old MDM | `macOS/` | `macOS/Troubleshooting/ADE-Enrollment-A.md` (Playbook 2 — the superseded pre-26 wipe-based MDM move, still the only option for ineligible devices), `EntraID/Troubleshooting/CrossTenant-A.md` (identity-continuity side of a tenant-to-tenant device migration during M&A) |
| Defender: Cloud Apps (MDA), Identity (MDI), Vuln Mgmt, Network Protection, WDAC | `Security/Defender/` | `Intune/`, `EntraID/` |
| Defender for Cloud Apps File Policy retirement (January 6, 2027) — migration to Purview DLP (detect-and-respond) and Purview auto-labeling (label-only, never tool-covered), the DLP-to-Purview migration wizard's Can/Partial/Cannot-migrate verdicts (SharePoint/OneDrive DLP-type policies only), non-Microsoft app (Box/Dropbox/Google Workspace/Salesforce) preview-gated manual path, mandatory Test with notifications landing mode, and the Governance Action Mapping table for actions with no Purview equivalent (Trash file, Expire link, Transfer ownership) | `Security/Defender/` (`FilePolicyRetirement-A.md`/`-B.md`, `Scripts/Get-FilePolicyRetirementAudit.ps1`) | `Security/Defender/MDA-A.md`/`-B.md` (Cloud Discovery/session/access policies — explicitly unaffected by this retirement), `Security/Purview/DLP-Policy-A.md` (general Purview DLP reference for ongoing operation once a policy is migrated or recreated there) |
| Predictive shielding (Preview) — graph-based extension of automatic attack disruption that proactively hardens likely-next-target devices/accounts *during* an active incident, before confirmed compromise on that specific asset (GPO Hardening — blocks new GPO pushes only, not a rollback; SafeBoot Hardening — blocks boot-to-Safe-Mode; Contain User); no admin-authored policy object and no PowerShell/Graph configuration surface (autonomous, license- and feature-flag-gated only); the "Performed by: Attack Disruption" column ambiguity shared with standard reactive attack disruption, disambiguated only by action type and the alert-level Predictive shielding label; portal/Action-center-only undo, no confirmed cmdlet | `Security/Defender/` (`PredictiveShielding-A.md`/`-B.md`, `Scripts/Get-PredictiveShieldingAudit.ps1`) | `Security/Defender/AIR-A.md`/`-B.md` (Automated Investigation & Response — a separate MDE automation engine, alert-triage rather than graph-predictive), `Security/ExposureManagement/` (the standalone exposure-graph/attack-path product for manual analyst use, conceptually related but a different product) |
| Selective Response Actions — Defender for Endpoint onboarding-time restriction of cloud-initiated response actions for Tier-0/high-value assets (domain controllers, ADFS servers), via a Defender deployment tool (DDT) package choosing Full functionality vs. Restricted (four capability categories: Basic response, Advanced response, Live response, Device protection); Live Response script execution unconditionally blocked in Restricted mode regardless of category selection; immutable post-onboarding (offboard + re-onboard is the only reconfiguration path, device ID/history preserved); zero effect on detection/alerting/sensor coverage | `Security/Defender/` (`SelectiveResponseActions-A.md`/`-B.md`, `Scripts/Get-SelectiveResponseActionsAudit.ps1`) | `Security/Defender/PredictiveShielding-A.md`/`-B.md` (a distinct control — governs when Defender itself autonomously acts, not what a human/API caller may trigger), `Security/Defender/MDE-Onboarding-A.md`/`-B.md` (general onboarding mechanics unrelated to restricted mode) |
| Automatic Device Isolation (Preview) — confirmed via a live fetch of Microsoft's own "Take response actions on a device" page (`ms.date` 2026-07-23, `updated_at` 2026-08-25), specifically its "Isolate device - automatic attack disruption (Preview)" section, cross-referenced against community reporting (Bleeping Computer, 4sysops) confirming the May 2026 Defender for Endpoint ship date — the automatic-trigger extension of the long-GA manual "Isolate device" action, fired by automatic attack disruption against confirmed-compromised, onboarded end-user workstations only (explicitly NOT servers/domain controllers/unmanaged devices, which route through the sibling Contain critical assets/Contain devices/Contain IP actions on the same base page); scoped (targets only the specific incident device) and time-limited (auto-released after a defined window, or manually released); the two-tier, easily-conflated exclusion model — Selective isolation exclusions (per-device process/destination allow-list; defining one silently switches Full→Selective isolation type by default) vs. Automatic attack disruption exclusions (per-tag, per-action exemption via Policy applications and exclusions, shows Skipped status in Action center when triggered); a separately KB/build-gated forcible-release script (3-day expiry, specific Windows 10/11 builds only) distinct from the standard Release button | `Security/Defender/` (`AutoDeviceIsolation-A.md`/`-B.md`, `Scripts/Get-AutoDeviceIsolationAudit.ps1`) | `Security/Defender/PredictiveShielding-A.md`/`-B.md` (proactive hardening of *predicted* future targets — this topic is reactive containment of a *confirmed*-compromised device instead), `Security/Defender/AIR-A.md`/`-B.md` (per-alert automated investigation, a different automation engine) |
| AI Agent Runtime Protection (Preview) — Microsoft Defender for Endpoint inspects local AI coding agents (Claude Code, Codex CLI, GitHub Copilot CLI, GitHub Copilot app) at three points in the agentic loop (user prompt, pre-tool-call, post-tool-response) to detect and block prompt injection; two inspection methods — agent-native event inspection via vendor hooks (`Set-MpPreference -AiAgentProtection`) and network inspection for non-hooked agents (`-AiAgentNetworkInspection`, with documented certificate-pinning/HTTP-3 blind spots); no native Intune policy support yet (PowerShell platform script deployment only, and an unmodified script re-push is a no-op); protection does not apply retroactively to an agent process already running when the setting changes; distinct from the passive, no-config Local AI agent discovery inventory feature | `Security/Defender/` (`AIAgentRuntimeProtection-A.md`/`-B.md`, `Scripts/Get-AIAgentRuntimeProtectionAudit.ps1`) | `Security/Defender/AIR-A.md`/`-B.md` (conventional alert-triage automation, unrelated pipeline), `Security/Defender/AppGovernance-A.md`/`-B.md` (cloud/OAuth-app risk — a different AI/app security surface than local-agent runtime inspection) |
| Attack Simulation Training (phishing simulations, training assignment, MDO Plan 2) | `Security/Defender/` | `M365/Exchange/`, `EntraID/` |
| Purview DLP, sensitivity labels, insider risk, eDiscovery | `Security/Purview/` | `M365/Exchange/`, `M365/SharePoint-OneDrive/` |
| Communication Compliance (message review policies — no PowerShell for policy CRUD) | `Security/Purview/` | `M365/Exchange/`, `M365/Teams/`, `EntraID/` |
| Information Barriers (segment/policy design, Teams/SharePoint/OneDrive communication restrictions, Address Book Policy conflicts, segment overlap, FwdSync propagation delay) | `Security/Purview/` | `M365/Teams/` (primary enforcement surface), `M365/SharePoint-OneDrive/`, `EntraID/` (attribute source for segment filters), `M365/Exchange/` (Address Book Policy conflict, and the separate ethical-wall mechanism for mail flow) |
| Microsoft Priva — Privacy Risk Management (data overexposure/transfer policies, Test-mode default, Alert→Issue→Remediation) and Subject Rights Requests (Access/Export/Tagged list/Delete, portal-only, no PowerShell equivalent, data-residency exclusions) | `Security/Purview/` | `Security/Purview/DLP-Policy-A.md` (related but distinct — loss-prevention blocking, not proactive risk visibility), `Security/Purview/RetentionLabels-A.md` (regulatory records/holds must be checked before an SRR Delete request), `Security/Defender/DefenderForCloud-A.md` (Secure Score is a Defender for Cloud CSPM concept, distinct from Priva) |
| Microsoft Purview Audit (Unified Audit Log) — Standard vs. Premium retention/licensing, the 100/5,000/50,000 `Search-UnifiedAuditLog` result tiers, mailbox audit bypass, Management Activity API for SIEM-scale ingestion | `Security/Purview/` | `Security/Purview/Priva-A.md`, `Security/Purview/Insider-Risk-A.md`, `Security/Purview/CommunicationCompliance-A.md` (all three depend on the audit log as a hard prerequisite — this is the foundational layer under them, not a peer feature), `EntraID/` (Entra ID sign-in/audit logs are a separate, Entra-native log — not the same store) |
| Adaptive Protection — routes Insider Risk Management's ML-scored insider risk LEVEL (Elevated/Moderate/Minor, distinct from alert severity) into DLP, Data Lifecycle Management, and Conditional Access enforcement; all three arms ship safe-by-default (CA Report-only, DLP simulation mode) and require explicit promotion | `Security/Purview/` | `Security/Purview/Insider-Risk-A.md` (upstream signal source — troubleshoot there first if no levels are ever assigned), `Security/ConditionalAccess/` (the CA arm's policy design/troubleshooting, distinct from `insiderRiskLevels` condition wiring itself), `EntraID/Troubleshooting/IdentityProtection-A.md` (explicit disambiguation — `userRiskLevels`/`signInRiskLevels`/`Get-MgRiskyUser` are a wholly separate risk engine, frequently confused with Adaptive Protection's insider risk signal) |
| Graph API queries, automation, reporting | `EntraID/Graph/` | Any domain using Graph |
| macOS Intune enrollment, ADE, shell scripts | `macOS/` | `Intune/`, `EntraID/` |
| Entra ID PIM (directory roles/groups), MFA methods, SSPR, Identity Protection risk | `EntraID/` | `Security/ConditionalAccess/` |
| PIM for Azure Resources — JIT activation of Azure RBAC roles (Owner/Contributor/UAA/custom) at management group/subscription/RG/resource scope, via Azure Resource Manager API and `Az.Resources` (not Microsoft Graph) | `EntraID/` | `EntraID/` PIM for Directory Roles/Groups (shared portal shell, otherwise architecturally unrelated — different API, module, and permission model), general Azure RBAC (static/permanent assignments coexist with and can mask PIM-managed ones) |
| Entra Domain Services (managed domain), App Proxy, Access Packages | `EntraID/` | `Azure/`, `Security/ConditionalAccess/` |
| Entra Domain Services enhanced sAMAccountName synchronization (Public Preview) — sources sAMAccountName from onPremisesSamAccountName instead of mailNickname/UPN-prefix generation for hybrid users; Enterprise/Premium SKU only; dual-role RBAC gate (Application Administrator AND Groups Administrator); enabling on an EXISTING managed domain live-renames already-provisioned hybrid users' logon name during the next sync; Microsoft's own doc is internally inconsistent about the default state for newly created managed domains | `Azure/EntraDomainServices/` (`SamAccountNameSync-A.md`/`-B.md`, `Scripts/Get-SamAccountNameSyncAudit.ps1`) | `Azure/EntraDomainServices/EntraDomainServices-A.md`/`-B.md` (the pre-existing, always-on autogeneration baseline this feature modifies, not replaces), `EntraID/Troubleshooting/Connect-Sync-A.md`, `EntraID/Troubleshooting/CloudSync-A.md` (own the onPremisesSamAccountName sync pipeline itself) |
| GDAP (Granular Delegated Admin Privileges) — CSP/partner relationship access, security group role delegation | `EntraID/` | Partner Center (external) |
| Partner Tier1/Tier2 Support role retirement (MC1409305) — new-assignment block (HTTP 400) for two CSP/GDAP-scoped built-in roles, global rollout 2026-08-03 to ~2026-08-24, existing assignments unaffected | `EntraID/` | `EntraID/Troubleshooting/GDAP-A.md`/`-B.md` (GDAP Access Assignment role-mapping intersection point) |
| Company Branding custom CSS retirement (MC1435782/MC1458474) — two-stage SFI phishing-hardening retirement of layout/positioning CSS properties in sign-in branding, Stage 1 Jul 2026 new-usage block, Stage 2 Oct 2026 global enforcement | `EntraID/` | none — Entra External ID (CIAM) tenants explicitly excluded |
| Lifecycle Workflows — Entra ID Governance JML task automation (enable/schedule two-switch model, 3-day catch-up window, AD DS-synced Enable/Disable/Delete task prerequisites, Logic Apps extensibility) | `EntraID/` | HR-driven provisioning (external, creates the account), `EntraID/` Access Reviews (recertification, distinct), `EntraID/` PIM (role activation, distinct) |
| Windows Hello for Business, LAPS, Endpoint Privilege Mgmt (EPM) | `EntraID/` (WHfB) / `Intune/` (LAPS, EPM) | `Security/ConditionalAccess/` |
| Legacy Microsoft LAPS → Windows LAPS migration/coexistence — precedence rules (Windows LAPS policy always wins; legacy CSE absence silently triggers legacy-emulation mode against plaintext `ms-Mcs-AdmPwd`), immediate-transition vs. dual-account side-by-side coexistence paths, legacy software removal | `Intune/` (`Troubleshooting/LAPS-Migration-A.md`/`-B.md`, `Scripts/Get-LAPSMigrationStatus.ps1`) | `Intune/Troubleshooting/LAPS-A.md`/`-B.md` (steady-state Windows LAPS operation once already migrated — different scope, distinct file) |
| Azure Virtual Desktop (session hosts, FSLogix, App Attach) | `Azure/AVD/` | `Intune/`, `EntraID/`, `Windows/` |
| Azure Files (SMB/NFS shares, identity auth, Azure File Sync) | `Azure/Files/` | `DFS/`, `EntraID/` |
| Azure Storage Accounts (Blob/Queue/Table) — network/authn/authz gate model, Shared Key vs. Account/Service SAS vs. User Delegation SAS vs. Entra ID (RBAC), the control-plane-vs-data-plane RBAC split (Owner/Contributor grants zero data-plane access), soft delete/versioning, immutability (WORM) policies, lifecycle management tiering | `Azure/StorageAccounts/` | `Azure/Files/` (same account resource, different protocol/identity layer — SMB/NFS, not Blob/Queue/Table), `Azure/Backup/` (uses storage as a backend for some scenarios), `Azure/KeyVault/` (same RBAC-vs-legacy-authorization-model pattern) |
| Windows 365 Cloud PC (provisioning, ANC, resize/reprovision) — also covers Windows 365 Flex (pooled Dedicated/Shared licensing), Reserve (short-term on-demand access), Cloud Apps (published-app delivery), and Link (the purpose-built hardware thin-client device — Windows CPC OS, Entra-join + automatic-enrollment-only onboarding, SSO-mandatory connectivity, deliberately narrow Intune management surface with no Autopilot/app-management/malware-scan/remediation support, Bare Metal Recovery) | `Azure/Windows365/` | `Azure/AVD/`, `Intune/`, `M365/Licensing/` |
| Copilot for Microsoft 365 (licensing, data access, plugins) | `M365/Copilot/` | `M365/Licensing/`, `Security/Purview/` |
| Microsoft 365 Admin agent — the first-party, Microsoft-managed natural-language IT admin agent (Agent 365 Platform SDK, exclusive native MCP tool access), pre-installed for every admin at no additional Copilot/Agent 365 license cost, reachable from three surfaces sharing one cross-surface-continuous session (Copilot Chat, the M365 admin center's Copilot button, and a narrower SMB-scoped Microsoft Teams surface for ~300-user tenants, GA May 2026); the RBAC-reflection architecture (the agent has no standing privilege of its own — every read/write it performs mirrors the signed-in admin's existing Entra role assignments) and the mandatory per-write-action confirmation gate; the six documented task categories (users/licenses/groups, Copilot management, agent management, change management, help/support, Teams administration); and the inherited (not agent-specific) audit-log and data-privacy posture | `M365/AdminAgent/` | `M365/Copilot/AgentGovernance-A/B.md` (the shared Agent Registry control plane that blocks/scopes this agent and every other agent in the tenant — this topic covers using the agent, that one covers governing the fleet), `M365/Copilot/Copilot-A/B.md` (base Copilot licensing/CA/grounding, distinct product), `EntraID/` (the RBAC assignments this agent's every capability is gated by) |
| Legacy "Admin" app retirement (MC1462922) — the OLDER, unrelated first-party Teams/Outlook/Microsoft365.com app for very-small-business (VSB) tenants, two-stage retirement (Stage 1 mid-Aug 2026: no longer pre-pinned for new VSB admins; Stage 2 mid-Oct 2026: fully unsupported across all three surfaces regardless of prior install); no data/config loss (the app held no privilege or storage of its own, wrote through to the same backend as the full admin centers); Microsoft's own documented replacement path names the Microsoft 365 admin center, Teams admin center, AND the Microsoft 365 Admin Agent explicitly | `M365/AdminAgent/` (`LegacyAdminAppRetirement-A/B.md`, `Scripts/Get-LegacyAdminAppUsageAudit.ps1`) | `M365/AdminAgent/AdminAgent-A/B.md` (the CURRENT, unrelated AI admin agent in this same folder — easily confused by name alone with the app retiring here; this topic is the one being retired, that one is its recommended successor) |
| Viva Engage / Yammer — service-principal sign-in gate (tenant-wide, AppId `00000005-0000-0ff1-ce00-000000000000`), Native Mode architecture (community ↔ connected M365 Group ↔ SharePoint/OneNote/Planner, now universal since the Oct 13 2025 legacy-network retirement), the 7-role admin model split across 3 real assignment surfaces (Entra ID / Viva Engage-Yammer admin center / per-community), community-creation gating via the M365 Group creation policy (not a Viva Engage-native setting), and the rate-limited `/employeeExperience` Graph API surface | `M365/VivaEngage/` | `Security/Purview/CommunicationCompliance-A.md`/`RetentionLabels-A.md` (Viva Engage as a monitored/retained channel — Purview-side policy mechanics, distinct from this topic's access/admin model), `M365/Licensing/` (Group-based licensing covering Viva Engage Core/Premium) |
| Microsoft Places — hybrid-workplace app (desk/room booking, work plans, Places Finder/Explorer) layered on top of Exchange Online room/workspace mailboxes as a second, Places-native directory (Building>Floor>Section>Room/Workspace/Desk); the asymmetric Section-parenting rule for Workspace/Desk objects, the tenant-wide `EnableBuildings` visibility gate, the 3-role RBAC model split across Entra ID (Places Administrator) and Exchange RBAC (Places Building/Desk Administrator), the April 1 2026 licensing unbundling (Teams Premium → per-space MTR/MTSS/MTSS-SS model with a renewal-gated grace period), and the July 28 2026 automatic hierarchy-generation feature for hierarchy-less tenants. Requires PowerShell 7.4+ — will not run on Windows PowerShell 5.1 | `M365/Places/` | `M365/Exchange/` (underlying room/workspace mailbox + RoomList mechanics), `M365/Teams/` (Teams Rooms device/peripheral management, distinct admin surface), `M365/Copilot/` (Copilot room booking license), `M365/Licensing/` (per-user vs. this topic's per-space license model) |
| Viva Insights — the dual-model architecture splitting Personal insights (private, per-user, processed/stored inside the employee's own Exchange mailbox, never admin-visible) from Organizational insights (admin-configured, aggregated: Copilot Dashboard, Agent Dashboard, Consumption Dashboard, advanced analysis); the four-surface admin/access model (Insights Administrator, Insights Analyst, AI Administrator = real Entra directory roles; Manager, Group Manager = Viva-native only, never in Entra/PIM); Viva Feature Access Management (VFAM, `Get/Add/Update/Remove-VivaModuleFeaturePolicy`) as the sole current control surface for Copilot Dashboard/Agent Dashboard access since the older admin-center toggle and pre-VFAM PowerShell were retired; the two independent Minimum group size configuration surfaces split by license state (Copilot-only vs. Viva-Insights-licensed); and the tenant default-on/off setting vs. the separate End-user opt-out control (never applies to Copilot usage data) | `M365/VivaInsights/` | `M365/Copilot/Copilot-A/B.md` (Copilot licensing/CA/grounding broadly, not Viva Insights' own admin model), `M365/Licensing/` (Viva Insights add-on/Suite vs. Copilot-bundled licensing paths) |
| Microsoft Loop — the split between Loop components (embedded `.loop` blocks in Outlook/Teams/OneNote/Whiteboard, OneDrive/SharePoint license) and Loop workspaces (standalone Loop app, "Loop with workspaces" service plan); the two independent, non-overlapping admin policy tools (Cloud Policy at config.office.com — no PowerShell equivalent — governs everything except Teams; `Set-SPOTenant -IsLoopEnabled`/`-IsCollabMeetingNotesFluidEnabled` governs Teams only) plus a separate OWA mailbox policy gate for web Outlook/new Outlook; the single shared user-owned SharePoint Embedded container behind My workspace/Copilot Pages/Copilot Notebooks (always identified as application "Loop", `Get-SPOContainer -OwningApplicationId`); SharePoint Embedded container ownership types (User/Tenant/Group) and the Owner/Editor-only role model (Writer/Reader unused); a maturing Purview governance posture (retention labels only applicable inside the Loop app, no Information Barriers coverage for SharePoint Embedded, no end-user recycle bin for workspaces, eDiscovery custodian-picker rolling out from Aug 2026); the manual (non-automatic, unlike OneDrive) offboarding handoff for the personal container; no supported cross-tenant container migration; and the uneven cloud-environment availability matrix (Loop workspaces are commercial-cloud-only) | `M365/Loop/` | `M365/Copilot/Copilot-A/B.md` (Copilot licensing/CA broadly — this topic covers only the storage/container infrastructure shared with Loop), `M365/SharePoint-OneDrive/` (general SharePoint Embedded/OneDrive administration beyond Loop's specific use of it), `Security/Purview/` (underlying eDiscovery/retention/DLP/sensitivity-label mechanics) |
| Microsoft Planner — one brand, two architecturally separate products, three independent admin surfaces: Basic plans (Group- or groupless-Roster-backed, Azure/Exchange/SharePoint storage, the non-standard separately-downloaded `PlannerTenantAdmin` PowerShell module — `Set/Get-PlannerConfiguration`, `Set/Get-PlannerUserPolicy`, Global Admin only, MSAL.PS dependency) vs. Premium plans (formerly the standalone "Project for the web," retired as its own app Aug 2025 with zero migration into Planner, Dataverse storage inside a Power Platform Environment, controlled only via the M365 admin center's Project org-settings page — no PowerShell equivalent); the Dataverse dependencies unique to Premium (the "Enable D365 Apps" hard conflict, Default-vs-Production environment license minimums of 1 vs. 5, the customizable Project Team Member security role); Roster container self-deletion lifecycle; broad-by-default Basic-plan guest permissions (Group-layer only, no Planner-native tightening); the Exchange/SharePoint-dependency quirk in mobile Conditional Access enforcement; and the critical, easily-conflated distinction between Project for the web's completed Aug 2025 retirement (no action needed) and Project Online's separate, imminent Sept 30 2026 retirement (real migration required) | `M365/Planner/` | `M365/Copilot/Copilot-A/B.md` (Copilot licensing generally — Premium Planner's Copilot layer is additive), `M365/Loop/Loop-A/B.md` (shares the Cloud Policy mechanism gating the Planner Loop component), `PowerAutomate/Troubleshooting/DLP-Policies-A/B.md` (general Power Platform DLP mechanics underlying the Roadmap connector workaround), `EntraID/` (Group creation policy gates Basic-plan creation) |
| Project Online (SharePoint-based Project Web App/PWA) retirement — confirmed hard cutover September 30, 2026 (MC812729); new-SKU sales stopped ~Oct 2025, new PWA site creation blocked since Apr 1 2026, SharePoint-2013-workflow-dependent EPT governance broken since Apr 2 2026; SharePoint project-site content survives, Project Online's own structured schedule/resource-pool/timesheet data does not (no bulk-export/direct-SQL path); official export tool is per-user only, not tenant-wide; OData API dies independently (HTTP 410); no named migration destination (Planner Premium/Project Plan 3-5/Project Server Subscription Edition/Dynamics 365 Project Operations) is a full like-for-like replacement | `M365/ProjectOnline/` | `M365/Planner/Planner-A/B.md` (the UNRELATED, already-completed Aug 2025 Project-for-the-web-into-Planner retirement — easily confused by name alone; this topic is the one still needing migration) |
| Universal Print (queues, driverless printing) | `M365/UniversalPrint/` | `Intune/`, `EntraID/` |
| Teams Rooms devices, calling, meeting policies | `M365/Teams/` | `Intune/`, `EntraID/` |
| Passwordless Entra Resource Accounts for Teams shared devices (Teams Rooms on Windows/Android, panels, phones) — GA worldwide Aug 2026; converts stored username+password sign-in to a device-bound credential (WHfB-equivalent trust model); admin-initiated only via the Teams Rooms Pro Management Portal (no documented PowerShell/Graph endpoint for the migration action itself); the old password is NOT auto-removed (separate Cleanup Password wizard for cloud-only accounts, scramble-only for hybrid-synced accounts — password can never be fully deleted for those); platform-specific minimum OS/app version gates and an unconditional hybrid-join block for Teams Rooms on Windows; non-transferable device-bound credential destroyed on reset/re-image/replacement (re-provision with password, then re-migrate); Crestron TRW hardware and proxy-configured TRW devices unsupported at GA | `M365/Teams/` (`PasswordlessResourceAccounts-A.md`/`-B.md`, `Scripts/Get-PasswordlessMigrationReadiness.ps1`) | `M365/Teams/Teams-Rooms-A.md`/`-B.md` (initial resource-account provisioning and general MTR device management — this topic is a post-deployment authentication-method conversion layered on top, not a replacement), `EntraID/Troubleshooting/WHfB-A.md`/`-B.md` (the analogous device-bound-credential trust model for individual human users) |
| DFS Access-Based Enumeration, referral ordering / site costing | `DFS/` | `EntraID/`, `Windows/` |
| Windows Always On VPN, AppLocker, Credential Guard/VBS, Kerberos/NTLM | `Windows/` | `EntraID/`, `Security/ConditionalAccess/` |
| Intune Remote Help (helper/sharer remote assistance, tenant enablement, RBAC combo, licensing-both-sides, remote-launch notification delivery, elevation/unattended/CA) | `Intune/` | `Azure/Windows365/` (distinct connection stack — Remote Help can run *inside* a Cloud PC session but doesn't connect *to* one), `EntraID/` (Conditional Access service principal) — explicitly distinct from Graph's `remoteAssistancePartner` (third-party ISV onboarding) |
| Surface Management Portal (Intune admin center workspace for Surface-brand hardware — warranty/protection-plan coverage, support requests, service orders, device Insights, Security Copilot plugin integration) — not a separate license, gated only by an enrolled Surface device plus a Microsoft 365 admin role assigned via `admin.microsoft.com` (not Intune RBAC, not the Entra admin portal); the Microsoft Hardware Warranty Administrator/Specialist + Global Reader co-requirement (missing Global Reader silently underpowers the portal with no error); no comprehensive Graph API — only the compliance/encryption/storage/OS-eligibility slice of its Insights overlaps standard Intune Graph data | `Intune/` | `Autopilot/`/`Intune/Troubleshooting/Enrollment-A.md` (Surface hardware enrolls like any other Windows device — nothing enrollment-specific here), `Security/Copilot/` (general Security Copilot licensing/SCU economics, not the Surface-specific plugin) |
| Microsoft 365 Backup (SharePoint/OneDrive/Exchange protection policies, restore points, coverage gaps) | `M365/Backup/` | `Security/Purview/`, `M365/SharePoint-OneDrive/`, `M365/Exchange/`, `EntraID/` |
| Entra Verified ID (verifiable credentials — issuance/verification, DID/domain linkage, Admin API) | `EntraID/` | N/A (own Admin API, not Microsoft Graph) |
| SharePoint Advanced Management (SAM) — Restricted Access Control, Restricted Content Discovery, Site Lifecycle Management, Data Access Governance reports | `M365/SharePoint-OneDrive/` | `Security/Purview/` (sensitivity labels), `Security/ConditionalAccess/` (auth context, idle sign-out scoping) |
| Microsoft Sentinel data connectors (AMA/DCR, API/service, diagnostic-settings), ingestion gaps, workspace quota | `Security/Sentinel/` | `Security/Defender/` (XDR alert sources), `EntraID/` (sign-in/audit log diagnostic settings), `M365/Exchange/` (Unified Audit Log for O365 connector) |
| Microsoft Sentinel analytics rules & incident tuning (rule kinds, AUTO DISABLED, entity mapping, incident grouping, automation rules, false-positive tuning, Azure-vs-Defender-portal divergence) | `Security/Sentinel/` | `Security/Sentinel/` DataConnectors topic (data must be flowing before a rule can fire), `Security/Defender/` (Microsoft security rule kind sources) |
| Microsoft Sentinel Logic Apps playbooks / SOAR execution (automation rule → playbook handoff, permission/trigger-type model, connector auth, 3-layer throttling) | `Security/Sentinel/` | `Security/Sentinel/` AnalyticsRules topic (an incident/alert must exist before an automation rule can fire), `Security/Sentinel/` DataConnectors topic |
| On-prem AD DS replication (FSMO, repadmin/dcdiag, KCC/topology, lingering objects) | `ActiveDirectory/` | `DFS/` (SYSVOL is a separate replication system on the same DCs), `EntraID/` (Entra Connect sync depends on healthy on-prem AD) |
| AD-integrated DNS (zone replication scope, DC Locator SRV records, scavenging/aging, forwarders, split-brain) | `ActiveDirectory/` | `Windows/` (client-side DNS resolver config), `ActiveDirectory/` Replication topic (SRV records are a hard prerequisite for replication) |
| Azure Arc-enabled servers (Connected Machine agent onboarding, connectivity/heartbeat, 45-90 day identity expiry) | `Azure/Arc/` | `Security/Sentinel/` (Arc is a prerequisite for non-Azure server data connectors), `Security/Defender/` (CSPM/MDE on non-Azure servers), `EntraID/` (at-scale onboarding service principal credentials) |
| Microsoft Defender for Cloud (CSPM — Secure Score, recommendations, multicloud AWS/GCP connectors, agentless scanning, attack path analysis, regulatory compliance) | `Security/Defender/` | `Azure/Arc/` (on-prem/hybrid servers must be Arc-connected first), `Security/Sentinel/` (Defender for Cloud alerts feed Sentinel via data connector), `EntraID/` (subscription/tenant-scoped RBAC for Security Admin/Reader roles) |
| Azure Backup (Recovery Services Vault — Azure VM disk backup, recovery points, soft delete, immutability) | `Azure/Backup/` | `M365/Backup/` (distinct — SaaS data, not VM disks), `Azure/AVD/` (session hosts are VMs and use this same service), `Azure/SiteRecovery/` (shares the vault construct but is a distinct DR/replication service — snapshot backup vs. continuous replication) |
| Azure Site Recovery (Azure-to-Azure disaster recovery — continuous block-level replication, crash-consistent vs. app-consistent recovery point tracks, cache-storage-account data plane, four-stage failover/reprotect/failback state machine) | `Azure/SiteRecovery/` | `Azure/Backup/` (shares the Recovery Services vault construct but is snapshot-based backup, not continuous replication — the two are easily confused when a client says "vault issue"), `Azure/Compute/` VMExtensions topic (the Mobility Service replication agent is a standard VM extension and depends on the same VM Agent/WireServer layer) |
| On-Premises to Azure Disaster Recovery (VMware & Hyper-V — two unrelated on-premises component stacks: VMware's single ASR replication appliance vs. Hyper-V's Provider+Recovery Services agent installed directly on the host/VMM server; Classic VMware retirement 30 Mar 2026; Hyper-V non-recoverable-vs-recoverable error retry model; asymmetric outbound auth-proxy support) | `Azure/OnPremDR/` | `Azure/SiteRecovery/` (name-adjacent but a different direction of replication — Azure-to-Azure, not on-premises-to-Azure — confirm which one a DR ticket actually means), `Azure/Backup/` (shares the Recovery Services vault construct, distinct snapshot-backup service) |
| Workload identity federation (GitHub Actions/Azure DevOps/Kubernetes OIDC trust, secretless CI/CD auth) + Conditional Access for workload identities (direct service-principal targeting, risk-based blocking, Workload Identities Premium) | `EntraID/` | `Security/ConditionalAccess/` (same policy engine, workload-identity-specific constraints), `EntraID/` AppRegistrations topic (secret/cert-based auth this migrates away from) |
| Defender for Office 365 Safe Links (URL rewrite/time-of-click) and Safe Attachments (detonation) — policy precedence, Teams/Office app coverage, SharePoint/OneDrive/Teams separate toggle | `Security/Defender/` | `M365/Exchange/` (runs downstream of EOP anti-spam/anti-malware), `Security/Defender/` AttackSimulationTraining topic (training tool, not real-time protection) |
| Microsoft Entra Access Reviews (periodic recertification of group/app/access-package membership and Entra/Azure role assignments) | `EntraID/` | `EntraID/` PIM topic (role activation vs. periodic role recertification are distinct), `EntraID/` AccessPackages topic (entitlement management delivery vs. its own review lifecycle) |
| Catalog / User-centric Access Reviews (UAR, GA September 2026) — the person-centric counterpart to standard Access Reviews: a reviewer certifies one user's access across every resource in an entitlement management catalog (Groups/Teams, Applications, and disconnected Custom Data Provided Resources whose access data is supplied per-review-instance via CSV/Graph upload) in a single unified pass, completed in a separate My Access "Multi-resource" tab rather than the standard reviews list; no P1/P2-only license fallback (Governance/Entra Suite required outright), a documented 2-hour custom-data upload window from instance creation, a 12-hour pre-start catalog-membership freeze window, Custom Data Provided Resource reviews locked to single-stage/manager-only reviewers (no owner or self-attestation option for that resource type), and — the highest-value operational fact — Custom Data Provided Resource decisions do NOT auto-apply like groups/apps do: the calling organization must list denied decisions, remediate access in the actual disconnected system, then PATCH each decision item back via Graph, or the review instance sits in "Applying" indefinitely with no error surfaced | `EntraID/` (`Troubleshooting/CatalogAccessReviews-A.md`/`-B.md`, `Scripts/Get-CatalogAccessReviewAudit.ps1`) | `EntraID/Troubleshooting/AccessReviews-A.md`/`-B.md` (single-resource, resource-owner-driven reviews — the architecturally distinct predecessor mechanism this topic does not replace), `EntraID/Troubleshooting/AccessPackages-A.md`/`-B.md` (entitlement management delivery — catalogs can host access packages, but UAR reviews the catalog's groups/apps/custom-data resources directly, not access-package-level review policy) |
| AD FS / Web Application Proxy (on-prem claims-based federation for M365/SaaS — token-signing/decrypting certificate lifecycle, relying party trusts, claims rules, WAP proxy trust) | `ActiveDirectory/` | `EntraID/` (federation vs. Password Hash Sync/Pass-through Auth as the alternative identity model; HybridJoin/PRT-Issues topics for the post-token device auth path), `Security/ConditionalAccess/` (evaluated after AD FS issues a valid token) |
| Delegated Managed Service Accounts (dMSA, Windows Server 2025) — two-phase `Start-`/`Complete-ADServiceAccountMigration` state machine, client-side `DelegatedMSAEnabled` policy gate, BadSuccessor (CVE-2025-53779) privilege-escalation triage | `ActiveDirectory/` | `ActiveDirectory/` gMSA topic (shared KDS root key/GKDS password-derivation foundation; no gMSA↔dMSA conversion path exists), `Security/` (incident-response process for confirmed BadSuccessor exploitation) |
| Azure Key Vault (RBAC vs. legacy Access Policy authorization, network/private-endpoint access denials, soft-delete and purge-protection recovery, certificate auto-rotation) | `Azure/KeyVault/` | `EntraID/` AppRegistrations topic (client secrets as an alternative credential store), `EntraID/` WorkloadIdentity topic (federated credentials as a secretless alternative), `Security/Sentinel/` DataConnectors (vault diagnostic logs as a SIEM source) |
| Azure hybrid connectivity — VPN Gateway site-to-site (IPsec/BGP) and ExpressRoute (circuit provisioning, eBGP peering, customer/provider/Microsoft three-zone model) | `Azure/Networking/` | `Azure/AVD/` AVD-Connectivity topic (NSG rules scoped to AVD reachability specifically, not general hybrid connectivity), `Windows/` AlwaysOnVPN topic (a different, client-to-network VPN technology, not site-to-site), `Azure/Networking/` P2SVPN topic (individual-client VPN, a separate gateway configuration from this topic's site-to-site scope) |
| Azure VPN Gateway Point-to-Site (P2S) — customer-managed individual-client-to-VNet VPN: OpenVPN/SSTP/IKEv2 protocol layer, cross-constrained authentication types (Certificate, Microsoft Entra ID via Azure VPN Client — OpenVPN-only, RADIUS pass-through), client address pool sizing, root certificate lifecycle, Windows-vs-non-Windows client route-refresh asymmetry after topology changes | `Azure/Networking/` (P2SVPN) | `Azure/Networking/` HybridConnectivity topic (site-to-site — a different gateway configuration and failure domain), `Azure/Networking/` VirtualWAN topic (the User VPN/P2S gateway type embedded in a Virtual WAN hub — a separate, Microsoft-managed configuration surface), `Windows/` AlwaysOnVPN topic (Windows-native RRAS-based VPN, unrelated technology despite the shared "VPN" name) |
| Network Security Groups (general-purpose) — rule priority/evaluation order, dual subnet+NIC-level enforcement, service tags, Application Security Groups, Security Admin Rules via Azure Virtual Network Manager, NSG flow log retirement (Sept 30, 2027) | `Azure/Networking/` | `Azure/Networking/` HybridConnectivity topic (NSG as the shared final data-plane checkpoint both VPN and ExpressRoute converge on), `Azure/AVD/` AVD-Connectivity topic (NSG guidance scoped to AVD reachability specifically), `Azure/Networking/` Bastion topic (the recommended alternative to permanently-open management-port NSG rules) |
| Azure Bastion — managed RDP/SSH-over-TLS VM access without a public IP: four architecturally distinct SKU tiers (Developer shared-infra vs. Basic/Standard/Premium dedicated), the all-or-nothing 8-rule AzureBastionSubnet NSG requirement, the separate target-VM-subnet NSG requirement (most common real-world gap), connection-method availability by SKU (native client/IP-Connect/shareable links require Standard+; session recording/private-only require Premium), independent JIT access role-assignment layer | `Azure/Networking/` (Bastion) | `Azure/Networking/` NSG topic (the filtering layer Bastion's own required rules sit within), `Windows/` RDP topic (references Bastion as the recommended alternative to opening 3389 to the internet) |
| Azure Application Gateway (Standard_v2/WAF_v2) — inbound reverse-proxy/WAF: dedicated-subnet requirement, v2's GatewayManager 65200-65535 control-plane NSG dependency (missing it = blank/Unknown backend health for every server, not an explicit deny), listener/routing-rule/backend-HTTP-settings request path, default 200-399 probe-healthy matching, three-level WAF policy precedence (gateway/listener/path — most specific fully overrides, does not merge) | `Azure/Networking/` (AppGateway) | `Azure/Networking/` AzureFirewall topic (outbound/East-West TLS only — a different traffic direction from this topic's inbound reverse-proxy scope, despite both being "the firewall"), `Azure/Networking/` NSG topic (the control-plane 65200-65535 rule this topic's #1 real-world gap depends on) |
| Azure Load Balancer (Standard SKU) — Layer 4 (TCP/UDP) 5-tuple hash distribution (not round-robin, not client-affinity); Standard SKU's secure-by-default outbound model where a public LB's backend pool has ZERO implicit outbound Internet path (unlike the fully-retired Sept-30-2025 Basic SKU) until one of exactly three explicit methods is configured — LB outbound rules (default SNAT port allocation shrinks per instance as the backend pool grows), NAT Gateway (MS production recommendation), or an instance-level public IP; health probes blocking only NEW flows on failure (established TCP survives, all-pool-unhealthy UDP terminates immediately); HA Ports (internal-only, Floating IP/DSR mode selection for NVA scale-out) | `Azure/Networking/` (LoadBalancer) | `Azure/Networking/` AppGateway topic (Layer 7 — the resource most often confused with this one when a client says "the load balancer" but means path/host/WAF behavior this topic has zero awareness of), `Azure/Networking/` NSG topic (Standard SKU's explicit-allow requirement, unlike Basic SKU's old implicit permit) |
| Azure Front Door (Standard/Premium) — GLOBAL edge Layer 7: profile/endpoint/route/origin-group resource hierarchy; EXACT frontend-host route matching with NO wildcard-host fallback (stricter than the most-specific-wildcard-path matching that follows it); WAF policy is a separate resource with zero effect until a Security Policy associates it to a domain; WAF tier ceiling (Standard = custom rules only, Premium adds managed rules/DRS + Bot Protection + JS Challenge); custom domain validation and the apex-domain certificate-autorotation trap (no CNAME exists at a zone apex, so autorotation silently fails and needs recurring manual revalidation); Rule Set Route Configuration Override actions that can redirect to a different origin group than the route's own static config; fail-open round-robin when an entire origin group's health probes fail simultaneously; Classic tier retires March 31, 2027 | `Azure/Networking/` (FrontDoor) | `Azure/Networking/` AppGateway topic (regional Layer 7 — Front Door is global edge, often deployed together with AppGateway as a per-region origin), `Azure/Networking/` LoadBalancer topic (regional Layer 4, architecturally unrelated beyond both being traffic-distribution resources), `Azure/KeyVault/` (BYO-certificate scenarios reference the same cert-partnered-CA auto-rotation caveat documented there) |
| Azure Private Link / Private Endpoints — consumer-side connectivity to PaaS resources over a private VNet IP: the connection approval state machine (Pending/Approved/Rejected/Disconnected, only Approved carries traffic); the reserved `privatelink.*` DNS zone naming convention and Private DNS Zone Group automation (absent = the #1 real-world "resolves to the public IP" ticket); the hard rule against sharing one private DNS zone across two DIFFERENT services (each Zone Group reconciliation overwrites a conflicting record from another); six documented DNS resolution topologies spanning VNet/peered/on-prem with or without Azure Private Resolver; `PrivateEndpointNetworkPolicies` disabled by default — a structural exemption from NSG/UDR evaluation for PE traffic, not a permissive rule | `Azure/Networking/` (PrivateLink) | `Azure/Networking/` PrivateDNS topic (this topic's DNS layer depends on PrivateDNS's general zone/link mechanics — deliberately complementary, not duplicative), `Azure/Networking/` NSG topic (an NSG rule with "no effect" on a PE subnet is a PrivateEndpointNetworkPolicies question here before an NSG rule-priority question there), `Azure/KeyVault/` (network/private-endpoint access-denial scenarios reference this topic's connectivity layer) |
| Azure Front Door Premium — Private Link Origins — a REVERSED, Front-Door-specific application of Private Link: Front Door itself is the PE consumer, creating the endpoint from its own Microsoft-managed regional VNet and requesting approval from the origin owner (often a different team than whoever manages the FD profile); supported/unsupported origin types (App Service/Storage/Storage Static Website/ILB/APIM/App Gateway/Container Apps — explicitly NOT App Service Slots or Static Web Apps); Private Endpoint reuse keyed on resource ID+Group ID+region with a documented same-PE-different-port routing-issue trap; the mixed-public/private-origin-group constraint most often tripped by non-atomic multi-origin conversion; 7200 RPS-per-regional-cluster-per-profile rate ceiling for Private Link traffic specifically; mTLS unsupported to any Front Door origin, public or private | `Azure/Networking/` (FrontDoorPrivateLink) | `Azure/Networking/` FrontDoor topic (the general profile/route/origin architecture this layers onto), `Azure/Networking/` PrivateLink topic (the general connection-state-machine/DNS concepts this reverses the consumer direction of) |
| Just Enough Administration (JEA) — role-scoped PowerShell remoting endpoints layered on top of (not replacing) WinRM: role capability (`.psrc`) authoring (`VisibleCmdlets`/`Functions`/`Providers`/`ExternalCommands`, parameter/value scoping); session configuration (`.pssc`) authoring (`RunAsVirtualAccount`/`RunAsVirtualAccountGroups`/GMSA identity, `RoleDefinitions`, `RequiredGroups`); unconditionally-additive most-permissive-wins role-merge rules across every matching role; non-deterministic role-capability search order across modules with a same-named `.psrc` (Microsoft's own reason to keep names globally unique); Domain-Admins-by-default virtual account on a Domain Controller vs. local-Administrators-by-default on a member server; custom `FunctionDefinitions` bodies running OUTSIDE JEA's language-mode constraints by design | `Windows/` (JEA) | `Windows/` WinRM topic (the transport layer JEA sits on top of — "can't connect at all" is a WinRM ticket, "connected but access doesn't match expectations" is a JEA ticket), `ActiveDirectory/` KerberosDelegation topic (a GMSA-identity JEA session's own network-resource reach follows normal Kerberos delegation rules, not a JEA-specific mechanism) |
| Azure Virtual Network Manager (AVNM) — centralized network governance: scope/delegation model, static vs. dynamic (Azure Policy-based) network groups, mesh/hub-and-spoke connectivity configurations via the connected-group construct, goal-state deployment model (redeploying one config can silently drop another) | `Azure/Networking/` | `Azure/Networking/` NSG topic (Security Admin Rules — a different AVNM configuration type, evaluated before NSGs, fully documented there rather than in AVNM-A.md), `Azure/Networking/` HybridConnectivity topic (the peerings/gateways AVNM's hub-and-spoke configurations ultimately provision) |
| Azure Automation — managed identity authentication and the 30-Sept-2023 Run As account retirement, runbook job/Azure sandbox execution limits, extension-based Hybrid Runbook Worker connectivity (agent-based retired 31 Aug 2024); explicitly excludes Azure Update Manager, now its own dedicated topic below (Automation's own legacy Update Management is retired) | `Azure/Automation/` | `Azure/KeyVault/` (a common Automation target resource — Automation is absent from Key Vault's trusted-services firewall bypass), `Azure/Arc/` (non-Azure Hybrid Runbook Workers require a healthy Arc agent first), `EntraID/` AppRegistrations and WorkloadIdentity topics (alternative/complementary identity models to Automation's own managed identity) |
| Azure Update Manager — native, non-Automation-dependent patch management for Azure VMs and Arc-enabled servers: patch-extension lifecycle (Windows/Linux, lazy first-use install), on-demand vs. periodic vs. scheduled patching, maintenance configuration + configuration assignment as two independent resources (assignments don't survive an RG/subscription move), maintenance-window reboot-reservation arithmetic, short Resource Graph retention (7/30 days) | `Azure/UpdateManager/` | `Azure/Automation/` (the retired legacy Update Management solution this replaces — different architecture entirely), `Azure/Arc/` (hard prerequisite for patching non-Azure machines), `Windows/` Windows Update topics (the underlying WUA/WSUS client this service drives, not replaces) |
| AD CS Vulnerable Certificate Templates (ESC1 / ESC4) — direct, relay-free template misconfiguration privilege escalation: ESC1 (`CT_FLAG_ENROLLEE_SUPPLIES_SUBJECT` + client-authentication EKU + broad Enroll rights, letting a low-priv principal request a certificate for an arbitrary chosen identity) and ESC4 (non-admin WriteDacl/WriteOwner/WriteProperty on a template object, letting that principal reconfigure any template into an ESC1 shape); native `Get-ADObject`/LDAP-filter detection, template/ACL correction remediation | `Windows/` (ADCSTemplateMisconfiguration) | `Windows/` NTLMRelayADCS topic (ESC8 — a different attack primitive requiring coercion/relay, this topic requires neither), `Windows/` CertificateServices topic (auto-enrollment ACL plumbing — an availability concern, not a security-misconfiguration one, despite reading the same `nTSecurityDescriptor` attribute), `ActiveDirectory/Troubleshooting/CertificateMapping/` (KB5014754 — validates whether a *presented* certificate maps correctly; does not prevent obtaining a valid certificate via ESC1/ESC4 in the first place) |
| File Server Resource Manager (FSRM) — quota management (hard/soft, auto-apply, template propagation, nested-quota inheritance), file screening (file groups/templates/screens/exceptions, the Office .tmp save-then-rename gotcha), File Classification Infrastructure (USN Change Journal real-time vs. scheduled classification trade-off), file management jobs, storage reports | `DFS/` | `DFS/` Namespace and Replication topics (same file servers, architecturally separate role with no namespace/replication awareness), `Windows/` SMB topic (underlying share/NTFS permission layer FSRM sits on top of) |
| Azure Policy (resource governance/compliance — definition/initiative/assignment scope model, deny/denyAction/audit/auditIfNotExists/deployIfNotExists/modify effects with NO cross-assignment precedence, remediation task + managed-identity RBAC dependency, exemptions vs. notScopes, phased Azure Blueprints retirement beginning 31 July 2026) | `Azure/Policy/` | `Azure/Networking/` AVNM topic (dynamic network group membership uses Azure Policy-based conditions), `Security/Defender/` DefenderForCloud topic (regulatory compliance standards are themselves delivered as policy initiatives), `ActiveDirectory/` GroupPolicy topic (a different, unrelated "policy" system — same word, no shared architecture) |
| Azure Monitor Agent / Log Analytics (the telemetry pipeline underneath most of this repo's other Azure/Security monitoring topics — managed-identity/IMDS authentication, zero agent-side default config so collection is 100% Data Collection Rule-driven, Private-Link-only Data Collection Endpoints with a hard same-region requirement, Analytics/Basic/Auxiliary table-plan cost and query trade-offs; retired legacy Log Analytics/MMA/OMS agent backend shut down 2 Mar 2026 — any machine still on it has sent zero data since that date) | `Azure/Monitor/` | `Security/Sentinel/` DataConnectors topic (consumes data landed by this pipeline, but connector onboarding itself is documented there), `Security/Defender/` DefenderForCloud topic (its own legacy MMA-based auto-provisioning is a separate, mostly-superseded path from this pipeline), `Azure/KeyVault/` and `Azure/Networking/` NSG topics (send data via Diagnostic Settings directly to a workspace — a different ingestion path than the agent/DCR model documented here) |
| Azure Virtual WAN (Microsoft-managed global transit network — Basic/Standard SKU as a one-way capability upgrade, virtual hub with `ProvisioningState`/`RoutingState` as independent health signals, connection association/propagation via route tables and labels, Routing Intent/Routing Policies whose single biggest gotcha is silently taking over the Default route table and every connection's association on enable, secured virtual hub via Firewall Manager, VPN/ExpressRoute gateways sharing a fixed ASN 65515 inside every hub) | `Azure/Networking/` | `Azure/Networking/` HybridConnectivity topic (the same VPN/ExpressRoute protocols, but in the traditional self-managed hub-VNet model with different ASN handling and limits — not interchangeable with vWAN's embedded gateways), `Azure/Networking/` AVNM topic (can, in preview, target a Virtual WAN hub as its "hub" type — a governance layer orchestrating Virtual WAN, not a duplicate of its native routing model), `Azure/Networking/` NSG topic (still the filtering layer on spoke VNet subnets; a vWAN hub itself carries no customer-configurable NSG) |
| LSA Protection (RunAsPPL) — VBS-independent Protected Process Light isolation for lsass.exe, available since Windows 8.1/Server 2012 R2 with no SLAT/TPM/Hyper-V requirement; Windows 11 22H2+ silent auto-enablement writes no registry key (WinInit Event ID 12 is the only ground truth), Microsoft-signature + SDL-compliance gate for LSA plug-ins, audit (3065/3066) vs. enforcement (3033/3063) CodeIntegrity events, UEFI-locked configurations requiring the opt-out EFI tool to reverse | `Windows/` | `Windows/` VBS-CredentialGuard topic (complementary but independently-gated — a device can have either, both, or neither; do not conflate `lsaiso.exe`/HVCI troubleshooting with this topic), `Intune/` (Custom OMA-URI profile delivery path) |
| Microsoft Cloud PKI — fully cloud-hosted PKI for Intune (no NDES/Connector/on-prem CA), native and BYOCA (bring-your-own-CA anchored to a private root via CSR-sign-upload) deployment models, Microsoft-hosted SCEP registration authority, HSM- vs. software-backed keys, hard 3-CA-per-tenant capacity cap, bundled into Microsoft 365 E5 from 1 July 2026 | `Intune/` | `Intune/` Certificates topic (the older, architecturally opposite on-prem NDES/PKCS/Connector model — same end result, zero shared troubleshooting surface), `Azure/KeyVault/` (unrelated — Key Vault issues certs for Azure resources, not Intune-managed endpoints) |
| Intune Suite → Microsoft 365 E3/E5 base-licensing bundling (1 July – 1 Aug 2026 rolling tenant provisioning; E3-qualifying tenants gain Remote Help, Advanced Analytics, Intune Plan 2 features; E5-qualifying tenants additionally gain Endpoint Privilege Management, Enterprise Application Management, and Microsoft Cloud PKI) — tenant/per-user SKU eligibility, redundant standalone-add-on-spend review, licensing-only scope | `Intune/` | `Intune/` EPM, CloudPKI, RemoteHelp, and EnterpriseAppManagement topics (each covers its own feature's technical deployment/troubleshooting — this topic covers only the licensing entitlement mechanism feeding all four), `EntraID/` EntraSuiteLicensing topic (a different product family's own suite-to-tier consolidation — same pattern, unrelated licensing surface) |
| Windows Update Maintenance Window (Preview) — Update Policy CSP feature (`MaintenanceWindowEnabled` and related nodes) defining a strict, exclusive window for when download/install/restart update actions are allowed to run, architecturally distinct from Active hours' softer restart-suppression-only model; no native Intune Settings Catalog UI yet (custom Settings Catalog/OMA-URI profile required); gated behind a server-side, Known-Issue-Rollback-style feature-flighting switch entirely outside admin control, meaning a correctly-delivered policy can show "Succeeded" with zero observable effect; build-eligibility floor and CSP node behavior sourced primarily from independent reverse-engineering (Patch My PC) pending a fuller official Microsoft Learn conceptual article; under-documented interaction with existing Update ring deadlines | `Intune/` (`Troubleshooting/MaintenanceWindow-A.md`/`-B.md`, `Scripts/Get-MaintenanceWindowReadinessAudit.ps1`) | `Intune/Troubleshooting/WUfB-A.md`/`-B.md` (the established, GA Active hours/deferral/deadline model this feature supplements, not replaces), `Intune/Troubleshooting/FeatureUpdates-A.md`/`-B.md` (Update rings deadline mechanics relevant to the open precedence question) |
| Intune new single device page (console UI redesign) — becomes the default and sole device-details experience in the Intune admin center with the **September 2026 (2609) service release**, fully retiring the legacy multi-tab layout; presentation-layer only (no change to Graph data, compliance evaluation, or device actions), reorganizes content into Essentials/Device action status/Tools and reports/Properties/Device details, and enforces RBAC-driven action visibility by hiding (not greying out) ungranted actions — the leading cause of post-rollout "a button disappeared" tickets; service-controlled rollout with no tenant-level opt-out or legacy-page fallback | `Intune/` (`Troubleshooting/SingleDevicePage-A.md`/`-B.md`) | `Intune/` Enrollment, Policy-Conflict, and CustomCompliance topics (own the underlying data this page merely re-presents — route genuine data/policy discrepancies there, not here) |
| eSIM cellular profile deployment for Windows Connected PCs — two independent, non-interchangeable Intune bulk-activation methods: CSV activation-code import (public preview, Windows 10/11, one-time-use codes, `embeddedSIMActivationCodePools` Graph beta resource) and the newer, recommended eSIM download server/SM-DP+ FQDN method (Windows 11 only, Settings Catalog policy, EID-based, no individual codes); both share the underlying eUICCs CSP and the same hard architectural limitation — Intune's "Succeeded"/"Active" status confirms only its own delivery half, while actual profile download/activation is a device-to-mobile-operator handshake with no remote Intune visibility; no supported remote profile-removal action exists for either method | `Intune/` (`Troubleshooting/eSIM-A.md`/`-B.md`, `Scripts/Get-eSIMDeploymentStatus.ps1`) | `Windows/Troubleshooting/NetworkAdapters-A.md`/`-B.md` (general cellular/connectivity troubleshooting once a profile is confirmed active — out of scope here) |
| Microsoft 365 Apps deployment & update channels — Click-to-Run install architecture, Office Deployment Tool, Current/Monthly Enterprise/Semi-Annual Enterprise channel precedence (GPO > ODT > admin center > default), Shared Computer Activation, and the July 2026 SAC/MEC cadence unification | `M365/Apps/` | `M365/Exchange/` Outlook-Client topic (assumes this topic's Click-to-Run layer is healthy and covers Outlook-specific profile/Autodiscover issues only), `M365/Licensing/` (Entra ID license assignment, distinct from client-side activation) |
| Power Apps environments & Dataverse database administration (environment creation license/admin-role/capacity gates, the irreversible "Enable Dynamics 365 apps" database-creation decision, the three-portal — admin center/maker/Power Automate — independently-computed environment visibility model, solution import missing-dependency resolution for managed/unmanaged/deprecated components) | `PowerAutomate/PowerApps/` | `PowerAutomate/` flow-execution topics (a distinct failure domain — connector auth/throttling/DLP issues live there, not here), `EntraID/` (Entra ID group teams vs. legacy Dataverse owner teams for portal visibility) |
| Microsoft Secure Score (tenant-wide, security.microsoft.com/securescore — Identity/Device/Apps/Data category scoring, license-agnostic maxScore model, 24-48h/weekly/monthly refresh cadence variance, manual "resolved through third party"/"alternate mitigation" status overrides, Unified RBAC vs. legacy-Entra-role-gated Graph API access) | `Security/Defender/` | `Security/Defender/` DefenderForCloud topic (same name, entirely separate Azure-resource CSPM score via `Az.Security` — zero data overlap), `Security/Defender/` DefenderVulnMgmt topic (owns the Device category's underlying TVM exposure-score engine; this topic covers only how it rolls into the tenant score), `Security/ConditionalAccess/` (security-defaults-vs-custom-CA-policy overlap for the Identity category's MFA/legacy-auth controls) |
| Microsoft Security Exposure Management (MSEM) — the unified posture-management platform in the Defender portal aggregating endpoint (Defender for Endpoint/MDVM), cloud (Defender for Cloud CSPM — Azure/AWS/GCP), identity, external attack surface, and third-party (ServiceNow CMDB/Tenable/Qualys/Rapid7 — Preview) data into a single enterprise exposure graph; Public Cloud only (hard exclusion for Gov/China/sovereign clouds); a dual, simultaneously-supported, non-stacked RBAC model — Defender unified RBAC custom roles (`Exposure Management (read/manage)` under Security posture, requiring explicit assignment to the "Microsoft Security Exposure Management" data source, plus a separate `Core security settings (manage)` for EASM vendor changes) alongside 10 legacy Entra ID roles with a documented per-action permission matrix (Security Operator gets limited write, only Global Admin/Security Admin get near-full write) — whichever model is more permissive for a given user governs, with zero built-in cross-validation between them; device-group RBAC scoping gating full vs. restricted visibility and ALL manage-tier actions; a 72-hour ingestion-latency/14-day-minimum-retention/latest-snapshot-only data model (no historical trend beyond what Initiatives/Events explicitly track); Critical Asset Management (predefined classifiers spanning devices/identities/cloud that can only be toggled off not edited, custom query-builder classifications, manual asset review for low-confidence matches, and a hard Defender for Endpoint sensor-version floor of 10.3740.XXXX for device classification with zero error if unmet); Security Initiatives (metric-driven scores whose recommendation lists are a FILTERED VIEW over Microsoft Secure Score/Defender for Cloud recommendations, not an independently-tracked remediation system); and Attack Path analysis (hybrid on-prem+cloud choke-point identification, queryable via Advanced Hunting `ExposureGraphNodes`/`ExposureGraphEdges`) — sharing underlying graph-platform infrastructure with, but architecturally distinct from, both CIEM's Defender-for-Cloud-scoped Attack Path Analysis and Microsoft Sentinel's custom-GQL hunting graph | `Security/ExposureManagement/` (`ExposureManagement-A.md`/`-B.md`, `Scripts/Get-ExposureManagementRBACAudit.ps1`) | `Security/Defender/CIEM-A.md`/`-B.md` (Defender-for-Cloud-scoped Attack Path Analysis — a related but separately-configured/licensed sub-feature whose output feeds this topic's broader graph), `Security/Sentinel/SentinelGraph-A.md` (code-first custom-GQL hunting graph sharing platform infrastructure but serving analysts, not posture management), `Security/Defender/SecureScore-A.md` (the scoring surface this topic's initiative recommendations are a filtered view over — remediation registers there, not in MSEM) |
| Conditional Access Authentication Strengths (grant control requiring specific method combinations — built-in Multifactor/Passwordless/Phishing-resistant strengths vs. admin-defined custom combinations, device-bound WHfB vs. portable FIDO2, federated MFA trust via `federatedIdpMfaBehavior`, claims-challenge timing shared with Authentication Context) | `Security/ConditionalAccess/` | `Security/ConditionalAccess/` CA-Design and TokenProtection topics (strength is one grant control among several; token protection is a separate anti-replay layer evaluated after a strength challenge is satisfied), `EntraID/` WHfB topic (device-bound credential provisioning this strength type depends on) |
| Conditional Access Authentication Context (resource-tagging condition, `c1`–`c99`, deciding *when* a step-up fires vs. Authentication Strength's *what-satisfies-it*) — the four native consumer surfaces (Purview sensitivity label, direct `Set-SPOSite` tagging, Entra ID Protected Actions, PIM role Activation) with zero shared inventory across them, opportunistic/implicit ACRS token evaluation producing zero-visible-prompt-but-still-enforced behavior, PIM's documented backup-protection mechanism that only substitutes MFA when the paired CA policy is entirely missing (not when it's Off/Report-only/user-excluded), and SharePoint's permanent app-compatibility limitations list (OneDrive sync, Outlook, mobile apps, Teams webinar scheduling) | `Security/ConditionalAccess/` | `Security/ConditionalAccess/AuthenticationStrengths-A.md` (the complementary *what-satisfies-it* control, commonly paired as the grant on a context-targeting CA policy), `EntraID/` PIM/Protected-Actions topics (two of the four native consumer surfaces this topic covers in depth) |
| Entra ID Certificate-Based Authentication (native, non-ADFS X.509 smart card/PIV/CAC sign-in — CA trust chain of up to 250 uploaded authorities, CRL-only revocation checked from Entra ID's cloud-side validator not the client network, high-affinity `certificateUserIds` vs. legacy low-affinity UPN/RFC822Name binding, per-certificate-policy-OID authentication strength mapping for Conditional Access MFA satisfaction) | `EntraID/` | `EntraID/` WHfB topic (device-bound TPM key/cert, not a portable certificate — architecturally distinct despite both being "certificate-adjacent"), `Intune/` CloudPKI/Certificates topics (certificate *issuance*, consumed by but not part of CBA's own sign-in validation) |
| Managed Apple ID Federation with Entra ID (Apple Business, formerly Apple Business Manager — OIDC-linked domain federation backing a user's Managed Apple Account with their Entra identity; three-step Approve/Test/Turn-on setup, hard `userPrincipalName`-must-equal-email requirement with no alias/Alternate ID support, separately-toggled directory sync with a read-only attribute allow-list, Entra audit-log-driven forced session termination on password change/reset) | `macOS/` | `EntraID/` (the identity provider being federated to — device join/PRT is a separate, unrelated identity plane), `macOS/` Platform-SSO topic (device-level authentication extension, not the identity behind the Apple Account itself), `macOS/` ABM-Token-Renewal topic (device enrollment/VPP licensing tokens, unrelated to user sign-in identity) |
| Universal Print on macOS (native Universal Print Mac App, macOS Sonoma 14.6.1+ — separate client from the Windows-native experience, per-device-install vs. per-user-permission split, tenant-wide macOS Support and Document Conversion settings, cupsd.conf admin-requirement removal for non-admin printer installs, known-broken printer models requiring connector-based re-registration) | `M365/UniversalPrint/` | `M365/UniversalPrint/` Universal-Print topic (the Windows-native client and shared tenant-level printer/connector architecture this builds on), `macOS/` (device-side MDM/VPP app deployment) |
| Global Secure Access (GSA) client for macOS — system extension + transparent application proxy as two independently-gated components, two-profile MDM allow-listing, the June 2025 bundle-identifier migration (`com.microsoft.naas.globalsecure*` → `com.microsoft.globalsecureaccess*`) that traps fleets whose MDM profiles predate it, mandatory client-version floor (1.1.25070402+) before any macOS 26 upgrade, and the documented GSA/Explicit Forward Proxy coexistence conflict on macOS | `macOS/` | `EntraID/Troubleshooting/GlobalSecureAccess-A.md`/`-B.md` (tenant-side forwarding profiles, Private Access connectors, Conditional Access "Compliant Network" design — this topic assumes that layer is already healthy), `EntraID/` (device Entra registration via Company Portal, GSA's own auth prerequisite) |
| Global Secure Access (GSA) client for Windows (confirmed via a live Microsoft Learn fetch of the Windows client release history reference, `ms.date`/`updated_at` 2026-08-26) — the Windows-specific client counterpart to the macOS entry above: two core Windows services with a real rename lineage (Global Secure Access Auto Upgrade Service -> Policy Retriever Service -> Forwarding Profile Service, the last renamed at client v2.22.90 and given automatic restart-on-failure at v2.31.125; and Global Secure Access Management Service -> Engine Service at v2.8.45); the upcoming automatic-upgrade-via-Windows-Update delivery model starting November 2026, version-floor-gated (2.31.125 x64 / 2.32.294 Arm) with an explicit per-install `EnableWindowsUpdates=0` opt-out flag; the new **Prefer Local Network** setting (client v2.32.294, August 2026) that resolves a documented overlapping-local-subnet conflict with Private Access applications (printing/casting breakage); a hard, binary Hyper-V support boundary -- host-level traffic bypass for guest VMs works only with an **internal** virtual switch, and is explicitly NOT supported for an **external** virtual switch (requiring in-guest client installation instead); and a documented single-interactive-Windows-session limitation surfaced by its own dedicated health check test | `Windows/` | `EntraID/Troubleshooting/GlobalSecureAccess-A.md`/`-B.md` (tenant-side forwarding profiles, Private Access connectors, Conditional Access "Compliant Network" design -- this topic assumes that layer is already healthy), `macOS/Troubleshooting/GlobalSecureAccess-macOS-A.md`/`-B.md` (macOS-specific client -- shares the tenant-side forwarding-profile concept only; install/service/diagnostic mechanics are almost entirely separate) |
| Microsoft Entra Global Secure Access (GSA) — tenant/identity-side umbrella for Entra Internet Access (SWG-style M365/internet traffic protection) and Entra Private Access (ZTNA VPN replacement); traffic forwarding profile design, Private Access connector architecture (connector groups, quick-access apps vs. per-app segments), Conditional Access "Compliant Network" signal, and the shared Entra ID auth dependency both products inherit | `EntraID/` | `macOS/Troubleshooting/GlobalSecureAccess-macOS-A.md`/`-B.md` (macOS-specific client install/architecture — this topic assumes the tenant-side layer documented here is already healthy), `EntraID/Troubleshooting/AppProxy-B.md` (legacy on-prem App Proxy publishing — related but distinct from GSA Private Access, the two can coexist), `Security/Defender/` MDA topic (Defender for Cloud Apps session policies are a separate product, sometimes confused with GSA) |
| LDAP Signing & Channel Binding — the on-prem AD DS hardening that closes the NTLM-relay-to-LDAP privilege escalation path; `LDAPServerIntegrity` (0/None-1/Negotiate-2/Require) and `LdapEnforceChannelBinding` (0/Never-1/When supported-2/Always) as two independently-enforced controls, Event 2886/2887 (signing exposure) and Event 3039/3040 (channel binding exposure) diagnostics, why a TLS-terminating load balancer/proxy invalidates channel binding tokens by cryptographic design (not misconfiguration), and Microsoft's repeatedly-delayed default-enforcement timeline meaning the effective default cannot be assumed from memory | `ActiveDirectory/` | `Windows/Troubleshooting/SMB-A.md` (SMB signing — a parallel but protocol-separate relay-mitigation control, not interchangeable with LDAP signing), `Windows/Troubleshooting/Kerberos-A.md`/`NTLM-A.md` (the underlying auth protocols being relayed; this topic covers the LDAP-side control that blocks the relay's escalation step, not NTLM/Kerberos itself) |
| Certificate-Based Authentication Mapping (KB5014754) — the on-prem PKINIT/Schannel hardening closing CVE-2022-34691/26931/26923; the SID certificate extension (OID `1.3.6.1.4.1.311.25.2`) auto-added by Microsoft Enterprise CAs vs. explicit `altSecurityIdentities` weak/strong mapping types, Event 39/40/41 (missing mapping/predates account/SID mismatch) diagnostics, and the now-permanent, unbypassable Full Enforcement state on any DC patched with the September 9, 2025 update or later (the Compatibility-mode registry key is retired, not just re-defaulted) | `ActiveDirectory/` | `EntraID/Troubleshooting/CBA-A.md` (Entra ID's architecturally separate cloud-side Certificate-Based Authentication — different trust/validation model, not the on-prem KDC mechanism documented here), `Troubleshooting/LDAPSigning/LDAP-Signing-A.md` (a different NTLM-relay hardening on a different protocol, still has a live registry bypass unlike this topic) |
| Kerberos Armoring (FAST) — the pre-authentication-exchange hardening (Windows Server 2012+) wrapping AS-REQ/AS-REP in an encrypted, machine-TGT-derived armor channel; the domain-functional-level gate that silently no-ops "Always provide claims"/"Fail unarmored authentication requests" below Windows Server 2012, the three independently-configured KDC-side/client-side GPOs, down-level-DC-driven intermittent failures, and its role as the transport prerequisite for Dynamic Access Control claims/compound authentication and AD FS device claims | `ActiveDirectory/` | `EntraID/` (Windows Hello for Business Cloud Kerberos Trust/Microsoft Entra Kerberos — an architecturally unrelated feature sharing only the "Kerberos" name), `Troubleshooting/ADFS/ADFS-A.md` (AD FS configuration/claims-rule authoring itself, out of scope here — this topic covers only the Kerberos-layer prerequisite AD FS device claims depend on) |
| Kerberos Delegation (unconstrained, constrained/KCD via S4U2Self+S4U2Proxy, resource-based/RBCD) — the three impersonation-authorization models solving the classic "double hop" problem; classic KCD's `msDS-AllowedToDelegateTo` authorized on the front-end vs. RBCD's `msDS-AllowedToActOnBehalfOfOtherIdentity` authorized by the resource owner, the latter being the only model that crosses a domain boundary within a forest; the two unconditional blocking controls (`AccountNotDelegated`/"sensitive account" and Protected Users group membership, the latter also disabling NTLM fallback entirely); and unconstrained delegation's standing lateral-movement risk (full TGT caching) independent of any specific ticket | `ActiveDirectory/` (KerberosDelegation) | `Windows/Troubleshooting/Kerberos-A.md` (foundational ticket lifecycle/SPN mechanics this topic builds on), `Windows/Troubleshooting/NTLM-A.md`/`NTLMRelayADCS-A.md` (the separate NTLM-relay attack class, not to be conflated with Kerberos delegation's own double-hop problem), `ActiveDirectory/Troubleshooting/gMSA/gMSA-A.md` (delegation configuration mechanics for managed service accounts specifically) |
| Windows Server 2025 Hotpatch via Azure Arc/Update Manager — eligibility-gate/license-plane/delivery-plane architecture, VBS-policy-enabled-vs-actually-running distinction (same recurring gotcha as the Intune/Autopatch client-side hotpatch), quarterly baseline+two-hotpatch-month cadence and baseline-drift self-correction, the 19 May 2026 move to free Arc-enabled billing, and the two distinct October 2025 incidents (feature-licensing bug stalling enrollment vs. a mistakenly-distributed WSUS OOB update KB5070881) | `Azure/UpdateManager/` | `Intune/Troubleshooting/Hotpatch-A.md` (the architecturally separate Windows 11 client hotpatch via Windows Autopatch — same concept, different admin plane, licensing history, and baseline calendar), `Azure/UpdateManager/UpdateManager-A.md` (the general assessment/scheduling engine this topic's delivery plane rides on) |
| NTLM Relay to AD CS (PetitPotam / ESC8) — authentication coercion (PetitPotam/MS-EFSRPC and related PrinterBug/DFSCoerce/ShadowCoerce techniques) relayed to an AD CS Certificate Authority Web Enrollment/CES HTTP(S) endpoint lacking Extended Protection for Authentication, yielding a legitimately-issued certificate for the coerced identity — PKINIT logon as that identity and, when the coerced identity is a Domain Controller, DCSync/full domain compromise; EPA + HTTPS-only as the primary, non-negotiable mitigation (KB5005413/ADV210003) | `Windows/` | `ActiveDirectory/Troubleshooting/CertificateMapping/Certificate-Mapping-A.md` (governs whether a *presented* certificate maps to an account — architecturally distinct from this topic's concern of how an attacker *obtains* a legitimate certificate via relay), `ActiveDirectory/Troubleshooting/LDAPSigning/LDAP-Signing-A.md` (the parallel channel-binding relay-mitigation pattern on a different protocol), `Security/Defender/MDI-A.md` (Defender for Identity's AD CS certificate posture assessments provide proactive detection for this exact misconfiguration class) |
| Group Policy Central Store & ADMX/ADML Management — the SYSVOL-hosted `PolicyDefinitions` folder Group Policy Management Editor silently checks for and falls back per-machine (to local, OS-build-dependent ADMX) when absent; ADMX namespace collisions and ADMX/ADML version-pairing errors caused specifically by incremental copy-in-place updates rather than a full clean replace; the `EnableLocalStoreOverride` registry escape hatch; and the presentation-layer-only relationship between ADMX/ADML and the actual `registry.pol`-stored setting value (a missing/broken ADMX affects an admin's ability to view/re-save a setting, never client-side enforcement) | `ActiveDirectory/` (GroupPolicyCentralStore) | `Troubleshooting/GroupPolicy/AD-GroupPolicy-A.md` (GPC/SYSVOL replication and client-side GPO processing — this topic assumes that layer is healthy and covers only the ADMX/ADML editing-tool dependency layered on top), `Windows/Troubleshooting/GPO-A.md` (client-side processing on end-user machines, which has zero dependency on ADMX/ADML), `Intune/Troubleshooting/GP-to-CSP-A.md` (the Intune-native CSP alternative, out of scope here) |
| DNSSEC for AD-Integrated DNS Zones — zone-signing response-integrity control (RRSIG/DNSKEY/NSEC(3)) layered on top of, and architecturally independent from, AD DNS availability; the Key Master role (one signing server per zone) and its move-vs-seize recovery paths, the AD-integrated signed zone's in-memory-only (never-committed-to-disk) storage, forest-wide AD DS trust-anchor replication vs. `TrustAnchors.dns` on standalone servers, the manual-and-never-automatic DS record required for secure delegation to a parent zone, and Windows DNS Client's status as a non-validating stub resolver that trusts the server's AD bit rather than validating independently | `ActiveDirectory/` (`Troubleshooting/DNS/DNSSEC-A.md`/`-B.md`, `Scripts/Get-DNSSECAudit.ps1`) | `ActiveDirectory/Troubleshooting/DNS/AD-DNS-A.md` (zone replication scope, DC Locator SRV records, scavenging — the availability mechanics this topic assumes are already healthy and does not duplicate), `Windows/Troubleshooting/DNS-Client-A.md` (general client-side resolver config, not DNSSEC-specific) |
| Windows 11 Hotpatch (via Windows Autopatch) — restart-free monthly B-release security updates existing exclusively inside the Autopatch orchestration layer; a six-condition eligibility gate (license, 24H2+ build, current quarterly baseline, VBS actually *Running* not merely enabled, Arm64-only CHPE disablement, Intune Hotpatch-enabled quality update policy) that fails silently to the standard restart-requiring LCU rather than erroring; the quarterly baseline (Jan/Apr/Jul/Oct)-vs-hotpatch-month release calendar; the live May 2026 platform change flipping the tenant-wide default to Allow for every eligible device; and the absence of any automatic rollback for a bad hotpatch | `Intune/` (`Troubleshooting/Hotpatch-A.md`/`-B.md`, `Scripts/Get-HotpatchReadinessAudit.ps1`) | `Intune/Troubleshooting/Autopatch-A.md` (the general ring/deployment orchestration layer hotpatch rides on top of), `Azure/UpdateManager/ServerHotpatch-A.md` (Windows Server 2025 hotpatch — an architecturally separate, Azure Arc-managed product sharing only the name), `Windows/Troubleshooting/LSA-Protection-A.md` (adjacent VBS-dependent feature context, not this topic's own VBS deployment troubleshooting) |
| Azure Lighthouse (cross-tenant delegated resource management for MSPs/multi-tenant enterprises) — the two-ARM-resource-type architecture (`registrationDefinition`/`registrationAssignment`, the latter being what actually makes access live), the `authorizations` model's permanently unsupported roles (Owner, DataActions-bearing roles, custom roles) and the constrained User Access Administrator pattern via `delegatedRoleDefinitionIds` for managed-identity role assignment, eligible/PIM authorizations requiring at least one permanent authorization to coexist in the same offer, and the subscription/resource-group-only delegation scope (no native management-group scope, only an Azure Policy `deployIfNotExists` workaround) | `Azure/Lighthouse/` | `EntraID/Troubleshooting/GDAP-A.md` (the parallel but architecturally separate M365/Entra admin-role delegation system for CSP partners — zero overlap with this topic's ARM-only scope), `Security/Copilot/SecurityCopilot-A.md` (Lighthouse as one of three non-interchangeable MSSP access mechanisms for that specific product), `Azure/Policy/` (the policy-remediation scenario the constrained UAA pattern exists to support) |
| Microsoft Security Copilot — the three-layer RBAC model gating platform access (own Owner/Contributor roles, not Entra roles, with several Entra/Intune/Purview admin roles auto-inheriting Owner to guarantee a two-owner floor), Entra role inheritance, and on-behalf-of plugin/service-specific RBAC (Sentinel, Intune, Defender XDR, Purview each independently gated — the single most common "Copilot shows no data" root cause); Security Compute Unit (SCU) capacity — fixed clock-hour provisioned billing with no rollover, consumption-based overage, Microsoft 365 E5/E7 auto-provisioning, and the dual Azure-RBAC-plus-Entra-RBAC requirement to manage capacity; plugin/promptbook tenant-publish scope; and B2B tenant-switching/GDAP/Azure Lighthouse as three non-interchangeable MSSP access mechanisms | `Security/Copilot/` | `M365/Copilot/Copilot-A.md` (Microsoft 365 Copilot — an entirely different product sharing only the "Copilot" name), `Security/Defender/`, `Security/Purview/`, `Intune/Troubleshooting/`, `EntraID/Troubleshooting/` (each embedded experience's own service-specific RBAC, referenced but not owned by this topic) |
| Restricted Management Administrative Units (RMAU) — the special-cased AU (`isMemberManagementRestricted = true`, permanent at creation) blocking even Global Administrator/Privileged Role Administrator from directly modifying member Users/Devices/Security Groups (only these three object types eligible; membership is direct-only, never cascades) without a role explicitly assigned at the RMAU's own scope; the container-vs-member-modification split letting GA/PRA still manage the RMAU itself (including auditable self-assignment of a scoped role) while never touching members directly; the Global-Administrator-as-RMAU-member dead end (no AU-scopable role can reset a GA's own password); and the hard, documented incompatibility with PIM, Entitlement Management, Lifecycle Workflows, and Access Reviews | `EntraID/` (`Troubleshooting/RestrictedManagementAU-A.md`/`-B.md`, `Scripts/Get-RestrictedManagementAUAudit.ps1`) | `EntraID/Troubleshooting/PIM-A.md` (regular AU-scoped PIM policies — a distinct, compatible concept from this topic's RMAU/PIM incompatibility), `EntraID/Troubleshooting/AccessReviews-A.md`/`LifecycleWorkflows-A.md`/`AccessPackages-A.md` (the Governance features this topic's objects become unmanageable by once RMAU-protected) |
| Exchange Online Direct Send Abuse — the unauthenticated-by-design SMTP submission path (`<tenant>.mail.protection.outlook.com`) exploited by external attackers to spoof internal senders since ~May 2025 (70+ orgs reported), exploiting the fact that Direct Send mail is correctly classified `MessageDirectionality: Incoming`/`AuthAs: Anonymous` (NOT exempt from SPF, unlike genuine intra-org mail) yet still displays as an accepted-domain sender with no external-sender banner; the opt-in, default-off `RejectDirectSend` organization-config mitigation (550 5.7.68 TenantInboundAttribution); and Microsoft's April/May 2026 architectural-limitation stance (briefly deployed then rolled back an automatic mitigation, no committed platform-wide fix) placing the mitigation burden on each tenant | `M365/Exchange/` (`DirectSendAbuse-A.md`/`-B.md`, `Scripts/Get-DirectSendExposureAudit.ps1`) | `M365/Exchange/Mail-Flow-A.md` (uses the unrelated term "direct send" for a legitimate hybrid outbound-routing pattern — explicitly disambiguated in both files, zero conceptual overlap), `M365/Exchange/DMARC-DKIM-A.md` (SPF/DKIM/DMARC mechanics this topic assumes exist and focuses on hardening as a mitigation layer) |
| Microsoft Sentinel Threat Intelligence — the four STIX-typed TI import paths (Defender Threat Intelligence connector, the Upload API — current recommended path for custom/TIP integrations, workspace-scoped Entra app role required, STIX/TAXII connector, and the legacy Threat Intelligence Platform connector being deprecated); the **2025-07-31 schema cutover** from the single legacy `ThreatIntelligenceIndicator` table to `ThreatIntelIndicators`/`ThreatIntelObjects` — a silent, no-error migration where stale rule/query/workbook references simply stop seeing new data; ingestion rules' hard scope limit to connector-sourced objects only (never Upload API/manual); Id-based deduplication; and indicator-based analytics rule templates requiring the event-side data source to be independently populated | `Security/Sentinel/` (`ThreatIntelligence-A.md`/`-B.md`, `Scripts/Get-SentinelThreatIntelAudit.ps1`) | `Security/Defender/` (Defender for Endpoint's own, unrelated EDR-blocking Threat Intelligence Indicators feature — disambiguate first), `Security/Sentinel/Hunting-A.md` (TI as one input among many, not owned here), `Security/Sentinel/Watchlists-A.md` (a structurally different, non-STIX reference-data mechanism solving a visually similar problem) |
| Microsoft Defender Threat Intelligence (MDTI) Standalone Retirement — the August 1, 2026 shutdown of the separately-licensed, separately-browsed standalone MDTI portal and Intel Explorer experience (removed from purchasable Product Terms since 2025-10-01), disambiguated from both the Defender-portal destination it converged into (free entity enrichments via the Threat Intelligence Insights tab; license-gated Intelligence explorer/Intel profiles requiring M365 E5, an E5 Security add-on, Defender for Endpoint Plan 2, or a free Sentinel connector) and this repo's own pre-existing Sentinel TI ingestion-mechanics topic (unaffected by this retirement); the quiet rebrand to plain "Microsoft Threat Intelligence" in current Microsoft docs; a flagged conflicting community-post retirement date; and the CSP/MSP-specific credit-memo billing tail (lands with the partner, not the end customer) | `Security/Sentinel/` (`MDTIRetirement-A.md`/`-B.md`, `Scripts/Get-MDTIRetirementAudit.ps1`) | `Security/Sentinel/ThreatIntelligence-A.md` (the separate, unaffected Sentinel TI data-connector/ingestion mechanics this topic explicitly disambiguates from), `Security/Defender/` (Defender-portal entity enrichments/Threat analytics are the destination surface, not owned here) |
| Microsoft Sentinel Watchlists — the flat name-value-pair reference-data mechanism (NOT STIX-typed, NOT a detection feed) for allow/block lists and enrichment joins; the `SearchKey`-optimized query pattern via `_GetWatchlistAlias`/`_GetWatchlist()`; documented hard limits (10M active items workspace-wide across ALL watchlists combined, 3.8 MB local/500 MB Azure-Storage-preview upload, 3–64 char alphanumeric-bounded naming); the **retention-vs-refresh architecture** (28-day underlying Log Analytics table retention is routinely misread as an expiration date — the 12-day refresh cycle keeps actively-used watchlists perpetually current until explicitly deleted); Microsoft's own documented query-plane-vs-management-plane-divergence incident-triage guidance; and the explicit, permanent unsupported-via-Azure-Lighthouse cross-workspace management gap | `Security/Sentinel/` (`Watchlists-A.md`/`-B.md`, `Scripts/Get-SentinelWatchlistAudit.ps1`) | `Azure/Lighthouse/Lighthouse-A.md` (the delegated-access mechanism this topic's MSSP limitation is carved out against), `Security/Sentinel/ThreatIntelligence-A.md` (the STIX-typed sibling mechanism, architecturally unrelated despite superficial similarity) |
| Teams External Access, Guest Access & Shared Channels — the three architecturally distinct external-collaboration models (federation/no directory footprint/chat-call-meet only via `CsTenantFederationConfiguration`; guest access/B2B invite creating a real guest object in the tenant with full Team membership; shared channels/Teams Connect via Entra B2B direct connect with zero directory footprint and single-channel scope); the shared cross-tenant access policy backbone governing both guest access and shared channels (default policy vs. partner-specific overrides that **replace, not merge with**, the default); the mutual, independently-configured-on-both-sides requirement for B2B direct connect; `inboundTrust` for MFA/compliant-device claim acceptance; and the unsupported operation of adding a guest account directly to a shared channel | `M365/Teams/` (`ExternalAccess-A.md`/`-B.md`, `Scripts/Get-TeamsExternalAccessAudit.ps1`) | `EntraID/` (cross-tenant access policy is an Entra object, not a Teams one), `Security/ConditionalAccess/` (MFA/compliant-device trust interactions), `M365/SharePoint-OneDrive/Permissions-A.md` (guest file access has its own separate SharePoint-level sharing gate) |
| SharePoint Hub Sites — registration (`Register-SPOHubSite`) vs. association (`Add-SPOHubSiteAssociation`) mechanics; the core architectural principle that hub association governs **navigation, theming, and search scoping only — never permissions**, a frequent source of misconfigured-expectation tickets; the one-site-per-hub / one-level-of-nesting (hub-of-hubs) constraints; the 2,000 hub tenant registration ceiling with no available increase; approval-gated association via `RequiresJoinApproval`; and the 2-4h navigation/theme propagation delay plus up to 24h search-crawl delay that are routinely mistaken for broken configuration | `M365/SharePoint-OneDrive/` (`HubSites-A.md`/`-B.md`, `Scripts/Get-SPHubSiteAudit.ps1`) | `M365/SharePoint-OneDrive/Permissions-A.md` (the independent permission model each associated site retains), `M365/SharePoint-OneDrive/Advanced-Management-A.md` (the deliberate, separate mechanism — Restricted Access Control — for actual hub-wide access governance) |
| Azure Virtual Machines / Compute — VM Extensions & Boot Diagnostics — the VM Agent (Windows Guest Agent/WaAppAgent.exe/RdAgent service, Linux waagent) as the mandatory prerequisite layer underneath every extension; the WireServer (168.63.129.16:80/32526) control channel whose blockage silently breaks the Agent, every extension, AND Run Command simultaneously; VMExtensionProvisioningError/VMExtensionHandlerNonTransientError/stuck-in-Transitioning failure classes; CustomScriptExtension download-vs-execution-failure distinction; the Microsoft Antimalware extension's replace-not-merge exclusion-settings behavior; boot diagnostics as a hypervisor-level capability architecturally independent of the Guest Agent (works even when the OS never boots) and explicitly incompatible with Premium/ZRS storage accounts; Serial Console's hard dependency on boot diagnostics being enabled first; and Run Command (action vs. Managed) as a VM-Agent-dependent, not out-of-band, recovery path. Also covers the OS-level layer beneath it, **VM Boot & Disk Repair** — offline BCD repair (Generation 1/2 syntax split) for INACCESSIBLE_BOOT_DEVICE, stuck offline Check Disk, the three architecturally distinct reboot-loop causes (critical-service ErrorControl, bad update/LKGC, registry hive corruption), the manual snapshot/attach-to-repair-VM procedure vs. the automated `az vm repair` CLI flow, and Azure Disk Encryption's single-pass (v2+, automatable) vs. dual-pass (v1, manual-unwrap-only) version split | `Azure/Compute/` | `Azure/Monitor/` (Azure Monitor Agent extension shares this topic's extension-provisioning mechanics — Data Collection Rule association health is owned there, not here), `Azure/Backup/` (VM Agent health is a hard prerequisite for the Azure Backup VM extension), `Security/Defender/` (Microsoft Antimalware/MDE coexistence and exclusion conflicts on the same VM) |
| Universal Print Migration (on-premises print server → Universal Print) — the per-printer eligibility decision (Universal Print ready direct registration vs. Connector-required) made once at plan time; Connector sizing math (~700MB RAM per 100 registered printers, ~150 printers/B2s-class host, ~600/B2ms-class); registration-vs-sharing as two distinct objects/steps (the #1 "migration complete but invisible to users" gap); retiring the deprecated `PrintProvisioning.exe`+`printers.csv` tool and legacy GPO/logon-script delivery in favor of the Intune Settings Catalog Printer Provisioning policy (UniversalPrint CSP, Entra-Joined/Hybrid-Joined devices only — Workplace-registered/BYOD out of scope); and the running-but-disconnected legacy-server validation window before final decommission | `M365/UniversalPrint/` (`UP-Migration-A.md`/`-B.md`, `Scripts/Get-UPMigrationReadiness.ps1`) | `M365/UniversalPrint/` Universal-Print topic (steady-state operation once migration is complete — this topic assumes that layer, not the migration project itself), `Windows/Troubleshooting/PrintServerMigration-A.md`/`-B.md` (migrating between two on-prem print servers, i.e. NOT eliminating the server — an architecturally different PrintBRM-based tool and process sharing only the word "migration"), `Intune/` (Settings Catalog policy delivery mechanics this topic's client-deployment phase depends on) |
| Entra ID Enterprise Application (SCIM) Provisioning — outbound automatic user/group provisioning from Entra ID to third-party SaaS apps over SCIM 2.0 (or the on-prem provisioning agent for LDAP/SQL/REST-SOAP/PowerShell/ECMA targets); assignment-based scoping (direct/nested-never-read group membership) vs. attribute-based scoping filters (with a documented unsupported-attribute list); the matching-attribute join key as the single highest-impact setting; initial-vs-incremental cycle/watermark mechanics with a fixed, non-configurable per-app interval; quarantine detection (SCIM compliance/invalid credentials/escrow-proportion threshold — 5,000-failure floor, then 40%/40,000/60,000 thresholds — duplicate roles) and its 6h/12h/24h retry-then-28-day-auto-disable clock; and disable-vs-delete deprovisioning tied to soft-delete/hard-delete/unassignment/out-of-scope triggers | `EntraID/` (`Troubleshooting/EnterpriseAppProvisioning-A.md`/`-B.md`, `Scripts/Get-EnterpriseAppProvisioningAudit.ps1`) | `EntraID/Troubleshooting/CloudSync-A.md`/`-B.md` (same underlying provisioning engine, reverse direction — AD DS into Entra ID, not Entra ID out to a SaaS app), `EntraID/Troubleshooting/CrossTenant-A.md`/`-B.md` (a related but separately configured and separately quarantined Entra-tenant-to-Entra-tenant provisioning job type), `EntraID/Troubleshooting/AppRegistrations-A.md`/`-B.md` (the Service Principal's own credential/consent lifecycle, distinct from its provisioning job configuration) |
| Defender for Business (DfB) — the SMB-tier (≤300 user) licensing/policy layer running on the SAME SENSE sensor/portal as full MDE (not a separate agent); auto-provisioned Default policies (Next-gen protection + Firewall); 300-seat program cap enforced cloud-side, invisible to local sensor state; feature boundary vs. P2 (no Live Response, scoped Advanced Hunting, no custom detections); additive migration path to full MDE P1/P2/E5 | `Security/Defender/` (`DefenderForBusiness-A.md`/`-B.md`, `Scripts/Get-DefenderForBusinessStatus.ps1`) | `Security/Defender/MDE-Onboarding-A.md` (identical sensor/onboarding architecture — this topic covers only what's genuinely DfB-specific), `M365/Licensing/` (seat assignment/removal mechanics), `Security/ConditionalAccess/` (device-risk CA requires Entra join + Intune enrollment independent of DfB) |
| Microsoft Entra Agent ID — the identity/security framework (GA April 2026) extending Entra's identity, access, and governance model to AI agents as first-class nonhuman identities: the four-object model (agent identity blueprint → optional blueprint principal for multitenant agents → agent identity, inheriting the blueprint's delegated OAuth 2 scopes → optional 1:1 agent user for user-shaped resource access); the Owner (technical, users/service-principals only)/Sponsor (business, required at creation for identity+blueprint, restricted to Dynamic or Assigned-M365 groups if a group)/Manager (org-hierarchy, agent-user-only) administrative model, deliberately separate from and layered on top of ordinary Entra RBAC roles; verified role template IDs for Agent ID Administrator (`db506228-d27e-4b7d-95e5-295956d6615f`), Agent ID Developer (`adb2368d-a9be-41b5-8667-d96778e081b0`), and Agent Registry Administrator (`6b942400-691f-4bf0-9d12-d8a254a2baf5`); the documented, genuine overlap between Agent ID Administrator and AI Administrator for full agent-identity CRUD; entitlement-management access packages requiring an explicit "users, service principals, and agent identities" requestor scope (plus a second parallel "All Service principals" policy for legacy non-Agent-ID agents); Conditional Access (blueprint-level inheritance) and Identity Protection extension to agents, gated behind Microsoft Agent 365 licensing (bundled in M365 E7, add-on to E5/A5/Business Premium or Defender Suite + Purview Suite); sponsorship auto-transferring to a departed sponsor's manager while ownership has no such succession mechanism; and cross-platform onboarding for non-Microsoft agent platforms (AWS Bedrock, n8n) via the Entra ID Auth SDK sidecar or workload identity federation — architecturally distinct from the M365 admin center Agent Registry catalog/publish/governance layer, which aggregates Entra + Defender + Purview risk signals and whose Critical-severity "Shadow agent" risk fires on ANY of: no Registry entry, no owner, or no Entra Agent ID | `EntraID/` (`Troubleshooting/AgentID-A.md`/`-B.md`, `Scripts/Get-AgentIdentityGovernanceAudit.ps1`) | `M365/Copilot/AgentGovernance-A.md`/`-B.md` (the Agent Registry catalog/publish/approval layer — a different product surface that this topic's objects feed into but does not own), `M365/AdminAgent/AdminAgent-A.md`/`-B.md` (a specific first-party agent that reflects the signed-in admin's own Entra roles rather than using a standing Agent ID identity of its own), `EntraID/Troubleshooting/AppRegistrations-A.md`/`-B.md` (the pre-Agent-ID app registration/service principal credential model that bare, non-Agent-ID agents still rely on) |
| App Consent Policies & Illicit Consent Grant Attacks — the two-layer consent-governance model: the tenant-wide `authorizationPolicy` singleton (`defaultUserRolePermissions.permissionGrantPoliciesAssigned`, the master on/off switch) plus `permissionGrantPolicies` (built-in — `microsoft-user-default-low`/`-legacy`/`-recommended`/`-allow-consent-apps`, `microsoft-application-admin`, `microsoft-company-admin` — or custom, each an include/exclude condition-set pair), evaluated as a logical OR across every policy a user holds through any assigned role PLUS non-policy authorization mechanisms (Service Principal ownership, an `Application.ReadWrite.All`-holding caller) — a single approving path is sufficient, so tightening one policy alone never fully closes a gap; the admin consent workflow (`adminConsentRequestPolicy`) as a purely routing/visibility mechanism where being a designated reviewer grants **no RBAC of its own** (documented explicitly by Microsoft) — only Global Administrator/Privileged Role Administrator can approve Microsoft Graph application-permission (app role) requests specifically, a carve-out Application Administrator/Cloud Application Administrator do NOT get despite otherwise broad app-management rights; `microsoft-user-default-recommended`/`-allow-consent-apps` as living, Microsoft-managed policies whose rules change with no tenant-side log entry; and illicit consent grant (OAuth-phishing) attacks — an attacker-registered app tricking a user/admin into consenting, producing account-level data access that explicitly survives password resets and MFA re-enrollment since the attacker never needed the victim's credentials, detected via Purview Audit `Consent to application`/`IsAdminConsent` and the `ConsentType: AllPrincipals` tenant-wide-blast-radius signal, remediated via `Remove-MgOauth2PermissionGrant`/`Remove-MgServicePrincipalAppRoleAssignment` rather than any credential-reset playbook | `EntraID/` (`Troubleshooting/AppConsentPolicies-A.md`/`-B.md`, `Scripts/Get-AppConsentGovernanceAudit.ps1`) | `EntraID/Troubleshooting/AppRegistrations-A.md`/`-B.md` (assumes consent already exists and something else — credentials, the Service Principal — is broken; this topic is the trust/authorization decision itself), `EntraID/Troubleshooting/EnterpriseAppProvisioning-A.md`/`-B.md` and `WorkloadIdentity-A.md`/`-B.md` (both assume the app is already trusted/consented) |
| Microsoft Entra Administrative Units (regular, non-restricted) — the `directoryScopeId`-based role-assignment model (`/administrativeUnits/<id>` in place of tenant-wide `/`) that scopes a delegated role's permissions to only an AU's members, using the same `unifiedRoleAssignment` mechanism that Restricted Management AUs (RMAU) later narrow further; the AU-scope-eligible role list (Authentication/Attribute Assignment/Cloud Device/Groups/Helpdesk/License/Password/Printer/Privileged Authentication/SharePoint/Teams/Teams Devices/User Administrator, plus qualifying custom roles — Global Administrator and Privileged Role Administrator explicitly cannot be AU-scoped); the "adding a group to an AU manages the group object, never cascades to the group's members" rule, the single most common source of AU-scoping tickets; static vs. dynamic (rule-based) membership — dynamic AUs are single-object-type-only (users OR devices, never both, never groups), P1-licensed per matched member, capped at 3,072 rule characters and a tenant-wide combined 15,000 dynamic-group-plus-AU limit, and permanently reject manual add/remove once the rule is set; the non-admin-only anti-escalation carve-out blocking AU-scoped Helpdesk/Password/User Administrator from resetting passwords, reading/modifying auth methods, editing sensitive properties, or deleting/restoring ANY target who holds ANY directory role at all, unrelated or not; and the service principal/guest-user supplementary-tenant-wide-Directory-Readers gap, since neither principal type gets default directory-read permission the way member users do | `EntraID/` (`Troubleshooting/AdministrativeUnits-A.md`/`-B.md`, `Scripts/Get-AdministrativeUnitAudit.ps1`) | `EntraID/Troubleshooting/RestrictedManagementAU-A.md`/`-B.md` (the hardened RMAU variant — `isMemberManagementRestricted = true` — that blocks even Global Administrator/Privileged Role Administrator; builds on this topic's same role-assignment mechanism but is covered in its own dedicated pair of files, not duplicated here), `EntraID/Troubleshooting/AppConsentPolicies-A.md`/`-B.md` and `AppRegistrations-A.md`/`-B.md` (unrelated RBAC-scoping mechanisms that happen to share the `unifiedRoleAssignment`/Graph-role vocabulary) |
| Microsoft Graph Data Connect (MGDC) — the bulk, at-scale Microsoft 365 data-export service (into Microsoft Fabric, Azure Synapse Analytics, or Azure Data Factory) for enterprise analytics, architecturally distinct from the transactional Microsoft Graph API despite exposing the same underlying data; a granular dataset+column+user+sink consent model (vs. Graph API's whole-entity grant/deny) spanning Basic/Cleaned/Curated dataset tiers across Teams/Outlook/Entra ID/SharePoint-OneDrive/Viva Insights; the five-stage onboarding chain (Global Admin tenant-wide enablement toggle → Entra app registration whose OWNER — not the app itself — must be a non-guest user with an Exchange Online mailbox AND an E5-qualifying license, the single highest-value ticket-triage fact → dataset/column scope registered at `aka.ms/mgdcinazure` → consent approval restricted to Global Administrator specifically, excluding Application Administrator/Cloud Application Administrator despite their otherwise-broad app-management rights, with self-approval by the registering account blocked outright → pipeline run); the `Microsoft.GraphServices` Azure resource-provider billing gate (mandatory for all Fabric pipelines since Jan 31 2024, missing registration surfaces as an opaque "no authorization" error unrelated to RBAC); per-pipeline-run (not per-row) billing with always-round-up fractional units; the same-Entra-tenant-only boundary with zero cross-tenant support; the one-Office-region-per-pipeline hard constraint requiring dedicated per-region pipelines for multi-geo tenants; and two documented, non-bug silent extraction exclusions (on-premises/hybrid Exchange mailboxes, resource accounts) | `EntraID/` (`Graph/GraphDataConnect-A.md`/`-B.md`, `Scripts/Get-GraphDataConnectReadinessAudit.ps1`) | `EntraID/Graph/GraphAPI-BatchOperations-A.md`/`-B.md` (the transactional Graph API sibling — real-time/small-scope access to the same data, architecturally unrelated delivery mechanism), `EntraID/Troubleshooting/AppRegistrations-A.md`/`-B.md` (general app registration/service principal credential hygiene — this topic's app-owner-eligibility rule is a narrower, MGDC-specific overlay on top of that) |
| User.ReadBasic.All Graph permission scope security fix (September 2026) — Microsoft's own confirmation that this delegated permission has been accidentally granting read access to appRoleAssignments and license details beyond its intended basic-profile-only scope; least-privileged replacement is User.Read.All (covers both extras) or LicenseAssignment.Read.All (license-only); no published rollout date, unlike the same month's memberOf retirement | `EntraID/` (`Graph/ReadBasicAllScopeChange-B.md`, `Scripts/Get-ReadBasicAllUsageAudit.ps1`) | `EntraID/Troubleshooting/MemberOfRetirement-B.md` (same September 2026 change-announcement batch, hard Nov 3 2026 deadline by contrast), `EntraID/Graph/Useful-Queries.md` (broader Graph permission-inventory patterns) |
| Microsoft Fabric tenant/capacity administration — F-SKU capacity (Azure resource `Microsoft.Fabric/capacities`) replacing legacy Power BI Premium P-SKUs; capacity State (Active/Paused/Deleted) as the first triage step for tenant-wide outages; 7-day soft-delete recovery window on capacity deletion; workspace-to-capacity assignment (unassigned workspaces fall back to Pro-only behavior); CU-second billing/bursting/smoothing and the four-stage throttling policy (overage protection ≤10min → interactive delay 10-60min → interactive rejection 60min-24hr → background rejection >24hr, with carryforward/burndown and optional 3x-rate capacity overage preview); the full OneLake data-access-role (RBAC) model (Admin/Member/Contributor always bypass it via implicit Write — only Viewer/external is meaningfully restricted; RLS/CLS union-across-roles except SQL-Endpoint CLS which uses intersection/deny; propagation latency 5min for role definitions vs. up to ~2hr for group membership); Fabric domains (data-mesh governance/discovery grouping — explicitly does NOT affect workspace visibility or access, a common point of confusion; domain-contributor status and Workspace-Admin role are two independent requirements; reassignment blocked by default without a tenant-setting override); the domains REST Admin API surface (Create/Update/Delete Domain requiring an explicit `preview=false` flag; the 3 workspace-assignment operations — by ID/synchronous, by capacity/async LRO, by principal/async LRO-preview — all silently obeying the same override gate with zero error on a no-op; Role Assignments Bulk Assign/Unassign with narrower principal-type support for Admin than Contributor); the domains audit schema (13 OperationName values incl. 2 with undocumented property schemas; bit-flag-shaped `Value` enums on access/contributor-scope changes); Git integration (ADO/GitHub) full lifecycle — connect/initializeConnection/commit/update REST API surface with LRO polling (202 + x-ms-operation-id + Retry-After, poll /v1/operations/{id}); the one-direction-at-a-time sync state machine and `RequiredAction` (None/UpdateFromGit) decision point; the 6-state per-item Git status model (Synced/Conflict/Unsupported/Uncommitted/Update required/Needs update to last commit); the 3-switch tenant-setting delegation chain (tenant→capacity→workspace override precedence; ADO sync default-ON, GitHub sync default-OFF, cross-geo export ADO-only default-OFF); provider-specific limits (commit size 25MB SP/125MB SSO on ADO vs 50MB combined on GitHub; 1,000-item workspace/branch cap; logical-ID collision handling on InitializeGitConnection; the recycle-bin-restore-vs-Git-recreate duplicate-identity gotcha, where each path assigns different logical IDs to the "same" deleted item); and the Dec 1 2026 read-write-on-items enforcement change for sensitivity-labeled items. Deployment Pipelines (the separate stage-promotion ALM tool, complementary to but architecturally independent from Git integration) — 2-10 permanent-once-created stages; item pairing as the core identity mechanism (survives renames, never auto-merges same-named-but-unpaired items); autobinding same-workspace (fails the whole deploy if a dependency is missing from the target) vs. cross-pipeline (matches by stage INDEX not name, requires equal stage counts on both pipelines); deployment rules (data source/parameter/default-lakehouse, owner-only, apply starting the NEXT deploy only — never retroactively); the two-tier permission model (pipeline Admin is the only pipeline-level role and grants zero content access; deploying requires ALSO >= Contributor on both source and target workspaces); the Deploy Stage Content REST API as an LRO (202 + x-ms-operation-id/Retry-After, max 300 items/call, poll to terminal status); and the already-in-effect (Feb 12 2026, not upcoming) retirement of legacy/non-Enhanced semantic model metadata support, ASLC models exempted. Workspace Governance at Scale (tenant-wide workspace naming/lifecycle/creation-restriction governance, distinct from Domains' grouping scope and FabricAdmin's capacity/OneLake scope) — the Create Workspaces tenant setting (Everyone/specific security groups/Nobody, portal-only, no REST read); the confirmed absence of any platform naming-pattern-enforcement feature (only tenant-wide `displayName` uniqueness, a 256-char cap, and the reserved name `Admin monitoring` are actually enforced) with enforcement-by-restricting-creators as the only real lever; the 5-state portal lifecycle model (Active/Orphaned/Deleted/Removing/Not found) vs. the REST Admin API's 2-state `WorkspaceState` enum that reports orphaned workspaces as "Active" — the orphan-detection blind spot, requiring `Get-PowerBIWorkspace -Orphaned` or a zero-Admin `List Workspace Access Details` roster check instead of the `state` field; the "last Admin can't be changed" API guardrail and how it's bypassed by out-of-band Entra account removal; retention asymmetry (7-90 day configurable for collaborative workspaces vs. fixed 30 days for My workspaces, with My-workspace restore being a one-way conversion to a collaborative workspace type); Grant/Remove Admin Temporary Access (24h, My workspace only) vs. Restore Workspace as two non-interchangeable tools gated by Active-vs-Deleted state; and workspace-level Networking Communication Policy's defaultAction-defaults-to-Allow-if-omitted write-side gotcha. Explicitly NOT data engineering (pipelines/notebooks/Lakehouse authoring) — admin/capacity/governance/CI-CD-plumbing scope only | `Fabric/` (`FabricAdmin-B.md`/`-A.md`, `Domains-B.md`/`-A.md`, `GitIntegration-B.md`/`-A.md`, `DeploymentPipelines-B.md`/`-A.md`, `WorkspaceGovernance-B.md`/`-A.md`, `Scripts/Get-FabricCapacityHealth.ps1`, `Scripts/Get-FabricDomainAudit.ps1`, `Scripts/Get-FabricGitIntegrationStatus.ps1`, `Scripts/Get-FabricDeploymentPipelineStatus.ps1`, `Scripts/Get-FabricWorkspaceGovernanceAudit.ps1`) | `M365/SharePoint-OneDrive/` (OneLake is architecturally separate from SPO/OneDrive despite similar governance-setting patterns; Fabric domains are also distinct from SharePoint Hub Sites despite an analogous "grouping ≠ access" disambiguation), `M365/Licensing/` (per-user Pro licensing still required below F64 capacity), `Azure/` (F-SKU capacity provisioning is an Azure-subscription-layer operation) |
| App Governance (Microsoft Defender for Cloud Apps) — the OAuth-app risk/detection/remediation pillar inside Defender for Cloud Apps + Defender XDR, tracking non-Microsoft apps registered to Entra ID/Google Workspace/Salesforce while explicitly excluding Microsoft's own first-party apps (`AppOwnerOrganizationId` = tenant `f8cdef31-a31e-4b4a-93e4-5f571e91255a`); a two-independent-list RBAC model (turn-on-capable roles vs. a narrower view/manage-capable list that excludes Cloud App Security Administrator — the single most common "enabled it, can't see anything" ticket); the up-to-10-hour post-enablement data-population window and the undocumented both-portals (Cloud Apps AND Defender XDR) alert-provisioning gate; two alert types (always-on ML/anomaly threat detection vs. predefined/user-defined policy alerts, the former's underlying conditions changing on Microsoft's own schedule with no tenant-side log entry, mirroring Entra ID's living consent policies); policy-conflict resolution favoring the stronger action on overlap; and platform-asymmetric remediation — Microsoft 365 apps go through Entra-backed Ban/Disable app permissions/Disable app (Service Principal `accountEnabled=false`, blocking authentication entirely until reactivated), while Google Workspace/Salesforce apps go through a genuinely different Revoke app (Entra-side record only) plus Notify user (a manual end-user step app governance cannot force) | `Security/Defender/` (`AppGovernance-A.md`/`-B.md`, `Scripts/Get-AppGovernanceReadinessAudit.ps1`) | `Security/Defender/MDA-A.md`/`-B.md` (the broader Defender for Cloud Apps CASB product — Cloud Discovery, session/access policies, file policies — that app governance is one specific pillar within), `EntraID/Troubleshooting/AppConsentPolicies-A.md`/`-B.md` (governs whether a NEW consent grant is allowed in the first place; app governance operates only on grants that already exist and cannot be retroactively affected by tightening consent policy), `EntraID/Troubleshooting/AppRegistrations-A.md`/`-B.md` (credential/Service Principal health for tenant-owned apps, a different failure domain than OAuth-app risk) |
| Entra External ID for Customers (CIAM) — Just-In-Time (JIT) Password Migration — migrating end-user credentials from a legacy identity provider (or a retiring Azure AD B2C tenant, closed to new purchases since May 1 2025) into an External ID *for customers* tenant without a bulk password reset, via a customer-hosted `OnPasswordSubmit` custom authentication extension invoked only when a per-user `toBeMigrated` extension-attribute flag is true; the RSA-JWE encryption model (public key on the extension app's `keyCredentials`, private key held exclusively in Azure Key Vault and retrieved by the extension's Function via managed identity — never in app settings); the four documented response actions (MigratePassword/UpdatePassword/Retry/Block) and the `keyId` == `tokenEncryptionKeyId` consistency requirement whose mismatch fails decryption silently with no Entra-side error; the `disableStrongPassword` option as an explicit, Microsoft-documented time-boxed security trade-off (relaxes complexity only, never the 8-character minimum or expiry); `CustomExtensionThrottlingError`/`CustomExtensionTimedOut` as expected, batch-around-them failure modes at migration-wave scale rather than bugs; the listener policy's `priority`/`includeApplications` scoping and the hard endpoint-URL constraint (must be customer-hosted, never Graph/Entra/the legacy IdP's own sign-in page); and the three required RBAC roles (Application Administrator, User Administrator, Authentication Extensibility Password Administrator) | `EntraID/` (`Troubleshooting/CIAMMigration-A.md`/`-B.md`, `Scripts/Get-CIAMMigrationReadinessAudit.ps1`) | `EntraID/Troubleshooting/ExternalIdentities-A.md`/`-B.md` (workforce B2B/B2C guest collaboration — an entirely different External ID tenant configuration; that pair explicitly scopes out Azure AD B2C/customer identity, which is exactly what this topic covers instead), `EntraID/Troubleshooting/AppRegistrations-A.md`/`-B.md` (general app registration/credential hygiene — the extension app registration here is a narrower, JIT-migration-specific overlay) |
| Microsoft Entra ID Governance — Account Discovery (GA, confirmed via live Microsoft Learn fetch dated 2026-06-23) — retrieves the full user account list from an already-provisioned Enterprise Application's target system and correlates each against Microsoft Entra ID using the SAME matching attribute the provisioning job itself uses (a deliberate shared-join-key design that also means a broken matching attribute breaks both features identically); the hard DIRECT-mapping-only constraint on that matching attribute (Expression/Function-type mappings — otherwise fully supported for ordinary provisioning — fail correlation outright with Graph error code `MissingJoiningProperty`, confirmed via a live Microsoft Graph beta reference fetch); a four-value classification model where the portal's own how-to documentation names only three categories (Local accounts/Unassigned users/Assigned users) while the underlying Graph beta `correlatedIdentity.status` enum actually has four (`uncorrelated`/`correlatedNotAssigned`/`correlatedAssigned`, plus the undocumented-as-a-bucket `failToCorrelate` for a per-identity correlation-attempt error, distinct from a clean no-match); a dual RBAC/licensing gate (Entra ID Governance add-on or Entra Suite license, PLUS Application Administrator/Cloud Application Administrator/Hybrid Identity Administrator to TRIGGER a new run) that is deliberately narrower than the RBAC needed to merely READ a completed report via Graph (Global Reader, Reports Reader, and the Security Reader/Operator/Administrator family all qualify for reads only); a portal-trigger-only execution model with NO documented Graph POST/start endpoint — `GET /beta/reports/correlations` and its `/identities` sub-resource are read-only, confirmed via direct fetch of the List/Get API reference pages, not assumed; an explicit two-tier connector support matrix (confirmed-good: Atlassian Cloud, generic SCIM, Salesforce, SAP Cloud Identity Services, ECMA, GitHub Enterprise Cloud-with-caveats; explicitly unsupported: HR provisioning/Workday/SuccessFactors, ServiceNow, AWS, Snowflake, Cross-tenant synchronization, Cloud sync, Group provisioning to AD — two of which are separately-covered topics in this same folder); and bulk remediation via Microsoft's own `Assign-CorrelatedUsers.ps1` (rules-CSV-driven conditional access-package/app-role assignment, `-DryRun` supported and recommended-first) | `EntraID/` (`Troubleshooting/AccountDiscovery-A.md`/`-B.md`, `Scripts/Get-AccountDiscoveryReadinessAudit.ps1`) | `EntraID/Troubleshooting/EnterpriseAppProvisioning-A.md`/`-B.md` (the underlying provisioning layer Account Discovery is built on top of and shares its matching-attribute/quarantine failure surface with — not a standalone feature), `EntraID/Troubleshooting/CloudSync-A.md`/`-B.md` and `CrossTenant-A.md`/`-B.md` (both explicitly excluded from Account Discovery's supported-connector list despite being provisioning-adjacent topics in the same folder) |
| Cloud-Managed Remote Mailboxes (GA, writeback GA confirmed via live Microsoft Learn fetch dated 2026-08-03) — the `IsExchangeCloudManaged` per-mailbox Exchange-attribute Source of Authority (SOA) transfer that lets a directory-synced mailbox's Exchange attributes be edited directly in Exchange Online instead of requiring an on-premises "Last Exchange Server" (LES) `Set-RemoteMailbox` write plus a sync cycle; the two-tier attribute model — identity attributes (displayName, givenName, UPN, etc.) remain on-prem-sourced forever under this feature, only the narrower Exchange-attribute bucket transfers, a distinct and much narrower scope than the separate object-level SOA transfer feature for users/groups/contacts; Phase 1 (GA) per-mailbox transfer vs. Phase 2 (GA) writeback of a specific ~10-attribute allow-list (mail, proxyAddresses, extensionAttribute1-15, msExchExtensionCustomAttribute1-5, msExchRecipientDisplayType/TypeDetails — most cloud-editable Exchange attributes do NOT write back) via a Microsoft Entra Cloud Sync "EXO to AD attribute sync" configuration that runs alongside, not in place of, Entra Connect Sync; the Entra Connect Sync 2.5.190.0+ build prerequisite (older builds fail pushing to already-transferred mailboxes) and Cloud Sync provisioning agent 1.1.1107.0+ requirement for writeback specifically; the mandatory 24-hour-plus-one-sync-cycle wait after the last on-prem change before flipping the SOA flag (the top race-condition ticket cause); the `Mail`→`mail` writeback mapping auto-included only for configurations created on/after 2026-08-03, requiring a manual "Restore default mappings" for pre-GA configurations; and the tenant-wide `Set-OrganizationConfig -ExchangeAttributesCloudManagedByDefault` default, whose documented unsupported-sequencing failure mode (enabling before all on-prem mailboxes finish migrating) breaks new-mailbox provisioning with no self-service recovery path, Microsoft Support required | `M365/Exchange/` (`CloudManagedMailboxes-A.md`/`-B.md`, `Scripts/Get-CloudManagedMailboxAudit.ps1`) | `M365/Exchange/Hybrid-Coexistence-A.md`/`-B.md` (general hybrid topology/HCW/mail-routing — this topic assumes hybrid is already established and only covers the Exchange-attribute-editing layer within it), `M365/Exchange/MigrationBatches-A.md`/`-B.md` (mailbox move mechanics — distinct from Exchange-attribute SOA, which does not move the mailbox itself) |
| Microsoft Entra Connect Sync — Mandatory Upgrade / Version EOL Readiness (confirmed via live Microsoft Learn fetch of the version-history reference page) — the hard, one-time 30 September 2026 deadline below which ALL synchronization services stop tenant-wide for a server running below version `2.5.79.0` (a May 2025 back-end hardening release), kept strictly distinct from the separate, continuous per-version policy (in effect since 15 March 2023) under which every 2.x release retires exactly 12 months after its successor ships — a server can clear the mandatory floor and still be sitting on an individually-retired version; true-running-version detection (`miiserver.exe.VersionInfo.ProductVersion`, authoritative over Programs-and-Features install metadata, which can lag behind incremental back-end pushes) cross-checked against the ADSync module's own `Microsoft.Synchronize.ServerConfigurationVersion` parameter; the Microsoft-Entra-Admin-Center-only installer distribution model (no generic download-center URL); auto-upgrade eligibility as two independent gates (per-release publication status, plus the 2.6.3.0+ deliberate skip of any server with a manually modified `miiserver.exe.config`/`miisclient.exe.config`); the documented post-upgrade `System.IO.FileLoadException`/`System.Diagnostics.DiagnosticSource` known issue on upgrades to `2.5.190.0`/`2.6.1.0` for servers with a modified config file (commonly historical FIPS-mode Password Hash Sync guidance), with its exact binding-redirect workaround; the 2.6.79.0 recalled-installer case study; and current-release (`2.6.84.0`, 7 Jul 2026) operational changes carrying forward risk — Password Hash Sync self-healing removed (no longer auto-re-enables its cloud feature flag), App-Based Authentication setup no longer silently falls back to the legacy sync account, and `Set-ADSyncAADCompanyFeature`/`Set-ADSyncAADPasswordSyncState` now requiring explicit `-AADUsername` | `EntraID/` (`Troubleshooting/ConnectSyncUpgrade-A.md`/`-B.md`, `Scripts/Get-ConnectSyncVersionAudit.ps1`) | `EntraID/Troubleshooting/Connect-Sync-A.md`/`-B.md` (day-2 attribute sync errors, staging mode, and general Connect Sync operations on an already-current server — this topic is purely the version/upgrade-lifecycle layer, not sync-engine troubleshooting), `EntraID/Troubleshooting/CloudSync-A.md`/`-B.md` (the architecturally distinct lightweight-agent sync model this on-prem sync-engine topic does not cover) |
| Microsoft Entra Connect Sync → Microsoft Entra Cloud Sync Migration (confirmed via live Microsoft Learn fetch of the decision guide, migration FAQ, and step-by-step migration procedure pages) — the architectural transition itself, kept strictly distinct from both day-2 Connect Sync operations and day-2 Cloud Sync operations: Microsoft's April-2026-announced strategic mandate that all customers eventually move to Cloud Sync, under a Microsoft-phased, notification-driven eligibility rollout beginning July 2026 (tenant notified when eligible; determines timing, not whether); an independently-run readiness assessment — object scale (150K/domain) and group scale (50K members) caps, Hybrid Azure AD Join device-sync dependency (formerly a hard ✗ in Cloud Sync and the most common near-term blocker — now closed as of end-July-2026 by a preview-status `AD2AADDeviceSync` Cloud Sync feature, see the dedicated index entry below — with Cloud Kerberos Trust remaining the GA-recommended transition for production-critical dependencies), and Advanced Sync Rules/cross-forest references/attribute-merge/reconciliation (all ✗ in Cloud Sync, replaced only by a materially simpler expression builder) — scored independently of, and regardless of, any eligibility notification; the full ~29-row Connect-vs-Cloud-Sync feature comparison table reproduced from the live decision guide; the documented OU-scoped, staged migration mechanism, and its actual technical enabler, the `cloudNoFlow` custom Connect Sync inbound (join rule, target attribute `cloudNoFlow`)/outbound (link type `JoinNoFlow`, scope `cloudNoFlow = True`) rule pair — which stops Connect Sync from *exporting* (while still importing/joining, preserving rollback visibility) a migrating OU so a scope-matched Cloud Sync job can become that OU's sole writer, since running both tools against the *same* objects simultaneously is explicitly unsupported; the mandatory pre-migration Import/Export-settings configuration backup as the sole realistic rollback path; per-OU pre/post object-count and new-object-provisioning validation; the documented stop-then-soak-then-uninstall decommission sequence (never uninstall Connect Sync the same day as the final cutover); and the exception-request path (via Microsoft Support/account team) for a tenant unable to migrate within its assigned window | `EntraID/` (`Troubleshooting/CloudSyncMigration-A.md`/`-B.md`, `Scripts/Get-CloudSyncMigrationReadiness.ps1`) | `EntraID/Troubleshooting/CloudSync-A.md`/`-B.md` (steady-state Cloud Sync agent/provisioning operation — assumes migration is already complete), `EntraID/Troubleshooting/Connect-Sync-A.md`/`-B.md` (steady-state Connect Sync day-2 operation for a tenant staying on Connect Sync), `EntraID/Troubleshooting/ConnectSyncUpgrade-A.md`/`-B.md` (the separate, unrelated-clock 30 Sep 2026 version/EOL deadline that applies to a Connect Sync server regardless of migration plans, since migration itself is phased over time) |
| Microsoft Entra Cloud Sync Device Sync (Preview; confirmed via a live Microsoft Learn fetch of "Configure device sync with Microsoft Entra Cloud Sync (preview)", `ms.date`/`updated_at` 2026-07-21/2026-07-27) — closes what had been Cloud Sync's single most-cited feature gap against Connect Sync: a new `AD2AADDeviceSync` synchronization job type that reads AD computer objects and provisions corresponding Entra ID device objects (`DeviceTrustType` hardcoded to `ServerAd`, identical to classic Hybrid Azure AD Join) so devices can complete Microsoft Entra hybrid join; one-directional only (AD -> Entra ID), with device *writeback* remaining exclusively a Connect Sync capability and no Cloud Sync roadmap item for it; gated by a provisioning-agent version floor (1.1.1107+, below which the job type is not offered at all) and a per-AD-forest service connection point (SCP) — the same discovery object classic Hybrid Azure AD Join has always used, configured via Microsoft's documented `ConfigureSCP.ps1` (Enterprise Admins, ideally just-in-time access), which destructively clears and replaces any pre-existing `keywords` values rather than merging them; a fixed, non-customizable attribute mapping table with one genuine gotcha -- `RegisteredOwnerReference`'s `Once` mapping type, which only ever flows on the computer object's first sync and will not track a later `mS-DS-CreatorSID` change; three provisioning paths (the job's normal cycle, portal Provision on Demand, and Microsoft Graph `synchronizationJob`/`provisionOnDemand`); and a three-trigger deleted-device model (AD computer deletion triggers automatic Cloud Sync cleanup, but `dsregcmd /leave` or a manual portal delete do not trigger automatic re-creation without an AD-side change or explicit on-demand provisioning) | `EntraID/` (`Troubleshooting/CloudSyncDeviceSync-A.md`/`-B.md`, `Scripts/Get-CloudSyncDeviceSyncReadiness.ps1`) | `EntraID/Troubleshooting/CloudSyncMigration-A.md`/`-B.md` (the broader migration readiness framework this preview capability now feeds into as a no-longer-unconditional blocker), `EntraID/Troubleshooting/CloudSync-A.md`/`-B.md` (general Cloud Sync agent/provisioning health this feature is layered on top of) |
| Group Writeback v2 → Cloud Sync Group Provisioning Migration (confirmed via live Microsoft Learn fetch of the migration procedure page, `ms.date`/`updated_at` 2025-09-29/2026-06-25, and the Group Provisioning to AD DS feature page, `updated_at` 2026-02-26; the 30 June 2024 discontinuation date corroborated by three independent community sources — the Learn page itself states deprecation without restating the date) — a narrower, distinct migration from the general Connect Sync → Cloud Sync transition above: moving cloud-created security groups written back with **universal scope only** off the deprecated, unsupported-since-30-June-2024 Connect Sync Group Writeback v2 **public preview** feature onto Cloud Sync's Group Provisioning to AD DS feature; a hard scope exclusion for mail-enabled groups/distribution lists, which are not covered by this migration and continue on the still-supported (no announced EOL) Group Writeback v1 instead; the `msDS-ExternalDirectoryObjectID` reference-attribute prerequisite (copied from `adminDescription`, with Microsoft's own PowerShell copy/verify scripts transcribed in full) that Cloud Sync's global-catalog membership-reference validation depends on — the single most common root cause of a "migrated but membership is empty" ticket when skipped; an **attribute-scoped** (not OU-scoped) `cloudNoFlow`/`JoinNoFlow` coexistence rule pair — inbound join rule scoped to `cloudMastered EQUAL true AND mail ISNULL`, outbound `JoinNoFlow` rule scoped to `cloudNoFlow EQUAL true` — a genuinely different scoping mechanism than `CloudSyncMigration-A.md`/`-B.md`'s OU-based version, not an interchangeable template, with the same `JoinNoFlow`-isn't-full-staging nuance (reference/membership attributes can still flow) called out explicitly; the irreversible `Set-ADSyncAADCompanyFeature -GroupWritebackV2 $false` cutover switch, documented with no supported undo, carrying its own easy-to-miss side effect — all Microsoft 365 groups begin writing back to AD unconditionally afterward, independent of the admin-center "Writeback Enabled" toggle; Cloud Sync's default group-CN-naming format diverging from Connect Sync's own default (with the attribute-flow-expression override to preserve the old format transcribed from the live page); and Group Provisioning to AD DS's own separate scale ceilings (50K members/group; 10K-20K groups depending on scoping mode) | `EntraID/` (`Troubleshooting/GroupWritebackMigration-A.md`/`-B.md`, `Scripts/Get-GroupWritebackMigrationAudit.ps1`) | `EntraID/Troubleshooting/CloudSyncMigration-A.md`/`-B.md` (the broader, OU-scoped sync-engine migration this topic's coexistence mechanism deliberately does not share a rule-scoping template with), `EntraID/Troubleshooting/CloudSync-A.md`/`-B.md` (general Cloud Sync agent/provisioning job configuration once the migration itself is complete), `EntraID/Troubleshooting/AccessReviews-A.md` and `LifecycleWorkflows-A.md` (both mention group writeback only in passing, in the context of governing on-premises Kerberos apps via entitlement management access packages — a related but distinct Microsoft-documented scenario not duplicated here) |
| Azure AD B2C → Microsoft Entra External ID Migration (confirmed via live Microsoft Learn fetch of the standard-migration how-to page, `ms.date`/`updated_at` 2026-03-13/2026-06-15; the custom-policy-analyzer how-to page, `ms.date`/`updated_at` 2026-07-02/2026-07-07; and the plan/approach-decision page, `ms.date`/`updated_at` 2026-03-13/2026-04-28; the "supported until at least May 2030" commitment and the May 1 2025 new-purchase closure independently corroborated via a second `WebSearch` pass, consistent with this repo's standing two-source-date-corroboration discipline) — the project-level migration this repo's existing JIT-password-migration topic (`CIAMMigration-A.md`/`-B.md`) is only one credential-preservation option within, explicitly flagged as out of that pair's own scope note before this topic was built (confirmed via direct re-read rather than assumed); the feature-mapping model with no 1:1 lift-and-shift path (Custom policies/IEF XML → custom authentication extensions; hosted HTML/CSS pages → user flows + native authentication; user flows require no analysis at all, only custom policies do); the portal-only, read-only Migration Policy Analyzer (Identity Experience Framework blade in the SOURCE B2C tenant, requires a relying-party policy in the analysis selection, four-status output — Available/Custom Development Required/Not Currently Supported/Architecture Incompatible — with the latter two requiring a design decision, not a timeline wait); the standard-migration-vs-High-Scale-Compatibility(HSC)-mode decision gated on an approximate ~5,000,000-directory-object threshold, with HSC mode's feature gaps (no social identity providers, no passkeys, no age gating, no admin-portal experience, reduced Conditional Access — no authentication context/step-up/session controls/group-based app assignment, limited third-party fraud protection for web-hosted flows) framed as structural and durable for the life of the tenant in HSC mode rather than a temporary preview gap that later "catches up" to standard-mode parity; the three password-preservation patterns as two genuine mirror images plus a no-preservation baseline — JIT (External-ID-initiated, application cuts over first, credentials trickle in via `CIAMMigration-A.md`'s `OnPasswordSubmit` mechanism) vs. B2C-initiated (legacy-IdP-initiated credential harvesting via a B2C custom policy + REST API, credentials lead, applications cut over once enough of the base is migrated) vs. no preservation (SSPR/passwordless/plaintext-set-via-Graph); fresh-registration-only application cutover (B2C app registration objects are never reusable in External ID, and HSC mode additionally requires single-tenant-only `AzureADMyOrg` registrations); the documented gap that social/third-party identity providers configured *inside* B2C custom policies are not carried over automatically and must be rebuilt natively; age gating having no current equivalent in either standard or HSC mode; and the imminent, unrelated-clock 31 August 2026 retirement of User Insights (any new post-migration monitoring should go straight to Azure Monitor + Log Analytics) | `EntraID/` (`Troubleshooting/B2CMigration-A.md`/`-B.md`, `Scripts/Get-B2CMigrationReadinessAudit.ps1`) | `EntraID/Troubleshooting/CIAMMigration-A.md`/`-B.md` (the narrower, previously-existing JIT-extension-mechanics topic this project-level file is the parent of — hand off there once JIT is the chosen approach and the ticket is about the extension itself, not the surrounding project), `EntraID/Troubleshooting/ExternalIdentities-A.md`/`-B.md` (workforce B2B/guest collaboration — an entirely different External ID tenant configuration, not a migration-project topic at all), `EntraID/Troubleshooting/AppRegistrations-A.md`/`-B.md` (general app registration/credential hygiene once a fresh External ID app registration already exists) |
| Jamf Pro ↔ Microsoft Intune (macOS) — Compliance Connector coexistence + full migration (confirmed via live fetch of the current `setup-jamf-manually` and `troubleshoot-jamf.md` Microsoft Learn/support pages, and the official `microsoft/shell-intune-samples` macOS Migration Tools README; a genuine, unresolved inconsistency between Microsoft's own two live pages on the legacy Conditional Access deprecation date — January 31, 2025 per the more recently updated page vs. September 1, 2024 per the older support article — flagged explicitly rather than silently picking one) — two architecturally distinct scenarios kept strictly separate in the new files: (1) Compliance Connector coexistence, where Jamf Pro remains the MDM and Intune's Partner Device Management page only brokers compliance signal to Microsoft Entra Conditional Access, gated by an Entra app registration that must hold EXACTLY three application permissions (`update_device_attributes` against Intune, plus `Application.Read.All` against both Microsoft Graph and the legacy Windows Azure Active Directory API) — any additional permission documented as breaking registration outright (Cause 1); Include/Exclude group targeting with Exclude always winning; a macOS-compliance-policy-must-target-a-USER-group (not device-group) limitation; and the full seven-cause, Microsoft-documented troubleshooting tree (unresponsive devices via 24h check-in or Entra-token-refresh gaps, keychain sign-in prompts, license expiry, Jamf-Self-Service-bypass registering the device as Intune-native instead of Jamf-partnered, the integration toggle being off, and — the single highest-value fix — the full stale-registration cleanup procedure for duplicate/mismatched device records with its mandatory 30-minute wait); and (2) full unenroll-and-migrate onto Intune-as-MDM via Microsoft's community-maintained (not a supported product feature) `intuneMigration.sh` script, which authenticates to the Jamf Pro API (basic legacy user-token or OAuth API Roles/Clients on Jamf Pro 10.49+), issues an unmanage command, runs `jamf removeFramework`, and hands off to Company Portal — whose documented "Waiting for Intune" end state depends on two prerequisites the script itself does NOT create: the device's Apple Business Manager MDM-server reassignment to Intune happening BEFORE the script runs, and a separately-configured Intune onboarding experience | `macOS/` (`Troubleshooting/JamfMigration-A.md`/`-B.md`, `Scripts/Get-JamfIntuneMigrationAudit.ps1`) | `macOS/Troubleshooting/DeviceMigration-A.md`/`-B.md` (Apple's own MDM-agnostic, wipe-free Assign Device Management re-enrollment workflow for macOS 26+ — a genuinely different mechanism with no third-party API involved, not a variant of this Jamf-specific topic), `EntraID/Troubleshooting/AppRegistrations-A.md`/`-B.md` (general app registration/credential hygiene — the Jamf app registration here is a narrower, integration-specific overlay with an unusually strict exact-permission-count requirement) |
| Intune Remote Help — Windows Unattended Support with Remote Sign-In (Intune Suite Service Release 2608, August 2026; source: live Microsoft Learn Deploy/Plan/Start-a-session pages plus the Microsoft Tech Community launch blog) — sign in to a physical, corporate-owned, Intune-managed Windows device with no end user present, built on AVD remoting infrastructure rather than the attended Remote Help client (`RemoteHelp.exe`) — a distinct app stack and RBAC permission sharing only the tenant-wide `remoteAssistanceSettings` enable switch and Conditional Access hookup with attended mode; the unattended RBAC permission is deliberately excluded from every built-in role (a high-trust gate requiring explicit custom-role scoping, not an oversight); absolute, non-configurable device eligibility (physical/corporate-owned/x64/Entra- or hybrid-joined only — no Cloud PC/AVD/ARM64 override); a documented 30-second auto-start behavior when a user is present at the target device (intentional design, not a race condition); and an in-session authentication boundary fully independent from the admin-center identity used to launch the session, so a fully-RBAC-privileged helper can still be locally under-privileged inside the session by design | `Intune/` (`Troubleshooting/RemoteHelp-Unattended-A.md`/`-B.md`, `Scripts/Get-RemoteHelpUnattendedReadinessAudit.ps1`) | `Intune/Troubleshooting/RemoteHelp-A.md`/`-B.md` (attended Remote Help — shares only the tenant switch and CA hookup, not the connection mechanism or RBAC permission) |
| Intune Multi Admin Approval (MAA) — application-authenticated (Graph API/automation) enforcement (confirmed via live fetch of both current Microsoft Learn pages: "Use Multi Admin Approval with the Microsoft Graph API", `ms.date`/`updated_at` 2026-08-06, and "Use Multi Admin Approval in Intune", `ms.date`/`updated_at` 2026-07-30/2026-07-31, plus the `operationApprovalPolicy` Graph beta resource-type reference; a wrong AI-generated search-summary claim of "HTTP 403 Forbidden" independently caught and corrected against the live page's actual documented pair — HTTP 400 for a missing justification header, HTTP 412 Precondition Failed + an `x-msft-approval-code` response header as the correct "accepted, pending approval" signal, not an error — consistent with this repo's standing never-trust-a-cached-search-snippet-alone discipline) — the 2026 change extending MAA enforcement from delegated-only to also intercept app-auth (service principal/automation/third-party app) Graph API write calls against any access-policy-protected resource (Apps, Compliance policies, Configuration policies, Device actions, RBAC roles, Windows Scripts, Tenant Configuration), opt-in only in the sense that it requires a pre-existing access policy for the workload, with no separate toggle; the full four-step app-auth request/approval/resubmit cycle (`x-msft-approval-justification` Base64 header → 412 + `x-msft-approval-code` → separate-admin approval via the portal, applications can never self-approve → resubmission with `x-msft-approval-code` to actually apply the change); two approver-group conditions that fail completely silently with no error anywhere in the UI or API — wrong group type (must be a pure Microsoft Entra security group; Microsoft 365 groups, mail-enabled security groups, and distribution lists silently fail to resolve approvers) and missing direct (not nested, not via individual role assignment) RBAC role-assignment membership for the group object itself, the latter causing periodic automatic member removal; a 3-day request expiry with zero Intune notifications at creation, approaching-expiry, or approval; per-policy (not tenant-wide) service-principal exclusions, capped at 50 apps/policy, requiring a second admin's approval to add or remove, confirmed NOT exposed as a property on the documented `operationApprovalPolicy` Graph schema (portal-only as of this fetch); and the self-referential `role`-policyType RBAC deadlock (a Role access policy can block the very RBAC change needed to fix its own approver group's assignment) with its documented delete-wait-fix-recreate recovery sequence | `Intune/` (`Troubleshooting/MultiAdminApproval-A.md`/`-B.md`, `Scripts/Get-MAAAccessPolicyAudit.ps1`) | `Intune/Troubleshooting/ScopeTags-A.md`/`-B.md` (the underlying Intune RBAC/scope-tag permission model MAA layers an additional approval gate on top of, not a replacement for), `EntraID/Troubleshooting/AppConsentPolicies-A.md`/`-B.md` (app-registration permission/consent prerequisites — a `403` from a missing Graph permission is a different failure than the `400`/`412` pair this topic covers) |
| Windows Autopilot device association (GA, Intune August 2026 release; confirmed via live fetch of the current Microsoft Learn "Overview of Windows Autopilot device association" and "Requirements for Windows Autopilot device association" pages, `ms.date`/`updated_at` 2026-08-25/2026-08-27 on both, plus the Tech Community launch blog and cross-corroborating `WebSearch` coverage) — a Windows Autopilot **device preparation**-only feature (architecturally unrelated to classic hardware-hash Autopilot) that binds a physical device to the tenant *before* MDM enrollment by writing a TPM-attested tenant-affinity marker into UEFI firmware; the three-operation lifecycle — pre-associate (admin, in Intune, stores intent only, no UEFI write yet), associate (automatic on OOBE network connect, or technician-triggered manually; verifies TPM identity and performs the actual UEFI write), remove association (on-device PowerShell script only, run by admin/OEM/partner — **not exposed in Intune at all**, and the UEFI marker survives a plain OS reset/reinstall, a real gap for MSP hardware decommission/resale/tenant-transfer workflows); hard, no-exception eligibility gates (physical device only — zero VM support; TPM 2.0 present/enabled/not in Reduced Functionality Mode; Windows 11 24H2 or 25H2 + KB5120998; a fixed edition list); the features association specifically unlocks — device-targeted (not just user-group-targeted) policy assignment, pre-enrollment OOBE customization (language/keyboard, EULA/privacy page skipping, "Hide change account options" gated on Entra company branding being pre-configured, device-name templating via `%SERIAL%`/`%RAND:x%`), and automatic corporate-owned device marking that removes the need for the legacy manual corporate-identifier CSV upload; two current-release limitations stated as permanent for this release rather than bugs — Wi-Fi-connected OOBE never hides the language/keyboard pages (Ethernet-only for that behavior), and Windows 365 Cloud PCs are explicitly excluded (already trusted by a separate model) | `Autopilot/` (`Troubleshooting/DeviceAssociation-A.md`/`-B.md`, `Scripts/Get-DeviceAssociationAudit.ps1`) | `Autopilot/Troubleshooting/DevicePreparation-A.md`/`-B.md` (the underlying device-prep policy/Enrollment Time Grouping mechanics association layers on top of), `Autopilot/Scripts/Upload-Hash-Enroll2Autopilot.ps1` (classic Autopilot's unrelated hardware-hash trust model), `Azure/Windows365/` (Windows 365 devices are explicitly out of scope for this feature) |
| Intune STIG Audit Baseline (GCC High only; confirmed via live fetch of the current Microsoft Learn "Use STIG audit baselines to assess Windows device compliance in Microsoft Intune" page, `ms.date`/`updated_at` 2026-06-09/2026-07-01) — an audit-only (never enforcing) DISA Security Technical Implementation Guide compliance baseline for Windows 10/11, available **exclusively in US Government Community Cloud High tenants** — not commercial, not GCC, not DoD cloud — and gated by the separately-licensed Intune Advanced Analytics add-on on top of Plan 1/2; audits the full current benchmark (Microsoft Windows 11 STIG SCAP Benchmark, Version 2 Release 7, benchmark date 5 Jan 2026, 197 rules) as a single non-customizable profile with no CAT-severity or individual-rule subsetting, and UX-only profile creation with no documented Graph write endpoint; rides Intune's standard device-configuration delivery pipeline, so a co-managed device with ConfigMgr still owning the Device configuration workload silently never receives the audit policy at all; ~20 STIG rules (UEFI mode, admin-rights-list correctness, firewall presence, and others) are permanently excluded from automated evaluation since they require physical/administrative judgment no CSP can observe; results map to NIST XCCDF categories for formal DoD reporting, with up to 24h initial-population delay and 1-2 check-in cycles of ongoing lag; and a stark Graph API cost asymmetry between the per-setting cached-report pattern (≥591 calls for a full 197-rule pull) and the purpose-built bulk `exportJobs` pattern (2-3 calls, full tenant) | `Intune/` (`Troubleshooting/STIGAuditBaseline-A.md`/`-B.md`, `Scripts/Get-STIGAuditBaselineStatus.ps1`) | `Intune/Troubleshooting/Security-Baselines-A.md`/`-B.md` (the ordinary, all-clouds, push-and-enforce Windows MDM security baseline — architecturally opposite behavior despite sharing the same admin-center UI surface), `Intune/Troubleshooting/CoManagement-A.md`/`-B.md` (the Device configuration workload-ownership dependency this topic's delivery pipeline relies on but does not independently diagnose) |
| App Settings — native macOS/iOS binary & app launch control (macOS 27+/iOS-iPadOS 27+, supervised only; confirmed via live fetch of the current Microsoft Learn "App settings configuration for Apple devices" page, `ms.date`/`updated_at` 2026-08-20/2026-08-26, cross-corroborated with Apple's own `app.settings` DDM schema reference and deployment-guide WWDC26 app-management summary) — a Settings Catalog "Declarative Device Management (DDM) > App Settings" policy category with two independent halves sharing one delivery mechanism: macOS binary execution control (`Allowed Binaries`/`Denied Binaries`/`Always Allow Managed Apps`) enforced via Apple's Endpoint Security extension framework — the first native Mac control reaching command-line tools and standalone binaries, not just `.app` bundles, closing a gap third-party EDR products previously had to fill — and iOS/iPadOS app launch control (`Allowed Apps`/`Denied Apps`, bundle-ID based, replacing the now-deprecated legacy Restrictions-profile `Allowed`/`Denied app bundle IDs` settings); binary identifiers matched on `CDHash`/`SigningID`/`TeamID`/`PathPrefix`/`SigningState`, with a hard schema rule (`Allowed` entries require `CDHash` or `TeamID`; `Denied` entries require `CDHash`, `TeamID`, or `SigningID`) and `codesign -dvvv` as the only trustworthy source for these values; a silent, error-free hard gate (OS 27+ AND device supervision — no partial application, no warning for a device that fails either); `Always Allow Managed Apps`'s narrower-than-it-sounds scope (VPP + Line-of-business non-PKG/DMG only, explicitly excluding Intune-agent-installed LOB PKG/DMG apps); and no Microsoft-documented staged/report-only enforcement mode for this policy type (fail-closed from the moment it's assigned, unlike the audit-only-by-design Intune STIG baseline) | `macOS/` (`Troubleshooting/AppSettings-A.md`/`-B.md`, `Scripts/Get-AppSettingsAudit.ps1`) | `macOS/Troubleshooting/Gatekeeper-Notarization-A.md`/`-B.md` (signing/notarization trust check, runs earlier in the chain and is independent of this feature), `macOS/Troubleshooting/PPPC-A.md`/`-B.md` (permission-consent layer, runs after a binary is already permitted to execute; also covers the separate macOS 27 removal — not deprecation — of the pushed-Accessibility PPPC grant), `macOS/Troubleshooting/DDM-A.md`/`-B.md` (the shared DDM transport/Status-Channel layer this feature rides on, not duplicated here) |
| Windows 10 Extended Security Updates (ESU) — commercial Volume Licensing MAK activation + free entitlement paths (confirmed via live fetch of the current Microsoft Learn "Extended Security Updates (ESU) program for Windows 10" and "Enable Windows 10 Extended Security Updates (ESU)" pages, `updated_at` 2025-11-17/2026-04-22 respectively, cross-corroborated via `WebSearch` against multiple independent 2026 ESU-enrollment-troubleshooting sources) — a genuine gap in this repo's otherwise-saturated coverage, identified via a fresh `WebSearch` "what's new" sweep per the standing next-run instruction, then confirmed by precise (word-boundary) grep against the repo, since a naive substring grep for "ESU" false-positived against the word "resume" across dozens of unrelated files; the commercial/organizational program for Windows 10, version 22H2 physical devices past the October 14, 2025 end-of-support date — Volume Licensing MAK purchase (Year 1 = $61/device, doubling annually, cumulative, 3-year max, 1-license minimum), the `KB5066791`-then-`KB5072653` install-order-sensitive prerequisite pair, MAK install/activate via `slmgr.vbs /ipk` + `/ato` against one of three fixed Year 1/2/3 Activation IDs, and a 13-endpoint activation-server allowlist that's the most common silent activation blocker; explicitly disambiguated from three adjacent-but-different programs — the unrelated **consumer** ESU wizard (Microsoft Account-based, in-Settings, free/Rewards-points/$30 options), **Windows Server ESU** (separate SKUs/Arc-based free path), and the **free VM-host-side** entitlement already covered elsewhere (Windows 365/AVD/Azure VM/Azure Local/AVS/Nutanix, at no additional cost as the running OS) — while newly documenting the free **physical-endpoint** entitlement for devices connecting to a Windows 365 Cloud PC (the `EnableESUSubscriptionCheck` registry flag under `HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\SoftwareProtectionPlatform\ESU`, deployed via an Intune custom OMA-URI policy, a mechanism explicitly flagged as subject to change and cross-referenced to Microsoft's own live page rather than hardcoded as permanent); phone/Confirmation-ID activation and bulk VAMT Proxy Activation for air-gapped devices; and the structural, permanent gap that Microsoft stopped providing centralized tenant-wide ESU enrollment/reporting through its own admin tooling after October 15, 2025 — the specific reason the companion script exists, since no native fleet-wide report can be pulled instead | `Windows/` (`Troubleshooting/ESU-A.md`/`-B.md`, `Scripts/Get-ESUActivationStatus.ps1`) | `Intune/Troubleshooting/FeatureUpdates-A.md`/`-B.md` and `Intune/Troubleshooting/Autopatch-A.md` (Microsoft's own stated preferred outcome — moving devices OFF Windows 10 entirely — rather than treating ESU as a long-term destination), `Azure/Windows365/Windows365-A.md`/`-B.md` (the Windows 365 subscription entitlement the free physical-endpoint ESU path depends on) |
| Windows Recall (Copilot+ PC) — NPU-gated (≥40 TOPS) opt-in local screenshot/semantic-index feature; hard hardware eligibility gate with no policy bypass on non-Copilot+ hardware; the two-part prerequisite chain (BitLocker/device encryption + Windows Hello Enhanced Sign-in Security, a distinct, stricter trust tier from ordinary Hello sign-in) that blocks the enrollment toggle until satisfied; the `WindowsAI` CSP/GPO governance surface (`AllowRecallEnablement` for enrollment-level blocking, `DisableAIDataAnalysis` for capture-level blocking even on an already-enrolled device); local-only storage architecture (`%LOCALAPPDATA%\CoreAIPlatform.00\UKP\`, no cloud sync, no current Purview/eDiscovery indexing); best-effort sensitive-content filtering (passwords, card numbers, private/InPrivate browsing auto-exclusion) vs. explicit app/site exclusion lists as the only deterministic control; and the storage-allocation-slider-vs-actual-disk-free-space distinction that silently stalls capture with no user-facing error | `Windows/` (`Troubleshooting/Recall-A.md`/`-B.md`, `Scripts/Get-RecallPolicyAudit.ps1`) | `Intune/` (Settings Catalog / `WindowsAI` policy deployment channel), `Security/Purview/` (current lack of Recall-store indexing — relevant to eDiscovery/legal-hold conversations) |
| Quick Machine Recovery (QMR) (GA on Windows 11 24H2/25H2; confirmed via live fetch of the current Microsoft Support "Quick Machine Recovery in Windows" page, `ms.date` 2026-07-10, and the Microsoft Learn "Quick machine recovery" configuration page plus its linked Recovery CSP reference, both `ms.date`/updated within the last 3 months) — cloud-connected boot-failure remediation layered on top of Windows Recovery Environment (WinRE)/Startup Repair, requiring zero special hardware (a deliberate contrast with the NPU-gated Windows Recall topic above); two independently-gated settings under the `Recovery` CSP's `QuickMachineRecovery` node — `EnableQuickMachineRecovery` (master switch) and `AutoRemediationSettings/EnableAutoRemediation` with its `SetRetryInterval` (0-4320 min) and `SetTimeToReboot` (1-4320 min) timers, which Microsoft's own CSP reference states must satisfy retry-interval <= time-to-reboot or behavior is undocumented; the management-state-dependent default fork that is the single highest-value fact in this topic — ON by default with a one-time scan on unmanaged/consumer devices (Home, or Pro not domain-joined/not MDM-enrolled), OFF by default on Enterprise/Education/domain-or-MDM-managed Pro, with an explicitly admin-set value becoming permanently sticky through any later management-state transition (implicit defaults follow state, explicit values do not); WinRE's hard network-stack limitation to wired Ethernet or WPA/WPA2-password Wi-Fi only, with **no 802.1X/Enterprise Wi-Fi support** — the primary real-world adoption blocker for wireless-only enterprise estates, addressable only via a dedicated WPA2-PSK fallback SSID pushed through the sibling `NetworkSettings/Wifi/{SSID}/WlanXML` CSP node; a confirmed, flagged-rather-than-silently-resolved discrepancy between Microsoft's own two published minimum-build figures (the consumer Support article states 24H2 build 26100.4700+, while the Recovery CSP reference states the higher 24H2 26100.8737+/25H2 26200.8737+); `reagentc.exe /getrecoverysettings` as the sole documented on-device verification command, which prints any configured WinRE Wi-Fi password in plaintext (both companion documents build in explicit redaction guidance); and test mode's restriction to Windows Insider Program Experimental-channel devices only, meaning there is no supported way to validate the real recovery experience on standard production hardware before a fleet rollout | `Windows/` (`Troubleshooting/QuickMachineRecovery-A.md`/`-B.md`, `Scripts/Get-QuickMachineRecoveryAudit.ps1`) | `Windows/Troubleshooting/Recall-A.md`/`-B.md` (the architecturally unrelated, NPU-hardware-gated AI feature this topic is deliberately contrasted against — no hardware gate exists for QMR), `Intune/` (Settings Catalog / Recovery CSP policy deployment channel, and the general MDM-managed-vs-unmanaged distinction this topic's default fork depends on) |
| Secure Boot 2011→2023 Certificate Transition (confirmed via live fetches of Microsoft's own actively-updated Secure Boot support hub: general overview KB 5062710, `lastPublishedDate` 2026-05-18; registry key reference KB 5068202, `updated_at` 2026-07-13; Windows Security app status KB 5087130, `updated_at` 2026-07-13) — the original 2011-issued Microsoft Secure Boot certificate chain (KEK, UEFI CA, Option ROM CA, Windows Production PCA) beginning to expire June 24-27, 2026 (KEK/UEFI/Option ROM) and October 19, 2026 (Production PCA, Windows boot loader signing); devices without the 2023 replacement certs keep booting and keep receiving normal Windows updates — no immediate-brick cliff-edge for the vast majority of hardware — but progressively lose new boot-chain security protections (Boot Manager updates, DB/DBX revocation updates, new boot-level vulnerability mitigations), with second-order exposure for BitLocker boot-integrity hardening and third-party bootloader trust; the documented registry-key deployment/status mechanism (`AvailableUpdates`=`0x5944` enterprise trigger value under `HKLM\SYSTEM\CurrentControlSet\Control\SecureBoot`, `UEFICA2023Status`/`UEFICA2023Error`/`WindowsUEFICA2023Capable`/`BucketHash`/`ConfidenceLevel` status keys under the nested `\Servicing` key, and the `\Microsoft\Windows\PI\Secure-Boot-Update` scheduled task that runs every 12 hours and requires a restart to complete the boot-manager swap); three parallel deployment paths (manual/fleet-tool registry, GPO/Intune policy writing the read-only `AvailableUpdatesPolicy` reflection key, and Microsoft-managed automatic rollout via `HighConfidenceOptOut`/`MicrosoftUpdateManagedOptIn`); the hard, unfixable-from-Windows hardware/firmware-incapable ceiling (`WindowsUEFICA2023Capable=0` persisting after deployment, requiring OEM firmware engagement or hardware refresh, not a retry); and the April 2026 Windows Security app green/yellow/red Secure Boot badge rollout, disabled by default on enterprise-managed devices via `HideSecureBootStates` to reduce notification noise | `Windows/` (`Troubleshooting/SecureBoot2023-A.md`/`-B.md`, `Scripts/Get-SecureBoot2023CertStatus.ps1`) | `Windows/Troubleshooting/BitLocker/BitLocker-A.md`/`-B.md` and `Windows/Troubleshooting/VBS-CredentialGuard-A.md`/`-B.md` (both already document Secure Boot as a hard prerequisite without covering this certificate-rotation mechanic), `Azure/` (Trusted Launch/Confidential VM certificate handling is managed independently by Azure and explicitly out of scope here) |
| Windows 11, version 26H2 (enablement package; build 26300.9278 entered Insider Release Preview August 27, 2026, GA expected late September/early October 2026, per a live fetch of the Windows Insider Blog release post) — the shared 24H2/25H2/26H2 servicing-branch model in which each nominal version's new features ship dormant inside ordinary monthly Cumulative Updates well ahead of their release, so the version flip itself is delivered by a small (~174 KB) enablement package (eKB) that only toggles feature flags plus the version/build/UBR values, requiring a single restart rather than the multi-hour media-based process of a traditional feature update; the hard, error-silent prerequisite-LCU dependency that is the single most common real-world root cause of "26H2 never shows up" tickets, since the eKB is simply not offered — with no error surfaced — until the specific Cumulative Update that shipped its dormant code is installed; the structural, complete ineligibility of any device still on Windows 11 23H2 or earlier, for which this entire eKB fast path does not exist and a conventional full feature update is required first; the three parallel deployment surfaces Microsoft has enabled for staged validation and rollout (Windows Update for Business client policy via the `TargetReleaseVersion`/`ProductVersion` CSP pair, WSUS approval, and Windows Autopatch), with Intune Feature Update policies plus Update Rings as the practical MSP-facing staged-rollout management layer retaining Pause/Resume/Rollback controls; the commercial-device-specific default-on behavior flip for Windows Settings Backup, app-specific taskbar actions, and File Explorer enhancements that activates automatically as part of the version bump with no separate deployment step — the most likely source of "something changed after a routine patch" tickets and the reason this topic cross-references `WindowsBackup-A.md`/`-B.md` rather than duplicating that feature's own full policy surface; the time-boxed `DISM /Online /Remove-Package` uninstall path, with no guaranteed indefinite rollback window; and the edition-split support lifecycle (Home/Pro/Pro Education/Pro for Workstations through October 2028 vs. Enterprise/Education/IoT Enterprise through October 2029) relevant to refresh-cycle budgeting conversations | `Windows/` (`Troubleshooting/Windows11-26H2-A.md`/`-B.md`, `Scripts/Get-Windows1126H2ReadinessAudit.ps1`) | `Windows/Troubleshooting/Windows Update/Update to Latest A.md`/`B.md` (the full feature-update path for any device below the 24H2/25H2/26H2 shared branch, structurally out of scope for this topic's eKB mechanism), `Windows/Troubleshooting/WindowsBackup-A.md`/`-B.md` (the Windows Settings Backup feature's own full policy surface, only its 26H2 default-state flip is covered here), `Intune/` (Feature Update policy + Update Rings as the practical staged-rollout management surface) |
| Passkey Default Authentication Rollout / SMS-Voice Retirement (confirmed via live Microsoft Learn fetch of "Passkeys by default and retirement of Microsoft-provided SMS and voice authentication" and its companion FAQ page, both `updated_at` Aug 2026; identified via a fresh `WebSearch` "what's new" sweep per the standing next-run instruction — this is a live, in-progress Microsoft-driven rollout starting the day before this run) — the tenant-independent, fixed-calendar Entra ID change auto-enrolling every SMS/Voice-enabled user into a passkey profile plus a Microsoft-managed Registration Campaign starting September 1, 2026, with a separate SSPR explicit-registration tightening on September 7, 2026, a temporary Graph `optOutSettings.passkeyDynamicMigration` opt-out that only covers the nudge phase, and a hard, opt-out-free blocking-enforcement date of February 1, 2027 for any user whose only MFA method remains SMS/Voice; architecturally distinct from day-2 Passkey (FIDO2) registration/sign-in troubleshooting, which stays in the existing `Passkeys-A.md`/`-B.md` pair | `EntraID/` (`Troubleshooting/PasskeyDefaultAuth-A.md`/`-B.md`, `Scripts/Get-PasskeyDefaultAuthReadiness.ps1`) | `EntraID/Troubleshooting/Passkeys-A.md`/`-B.md` (FIDO2 registration/sign-in mechanics and the CA phishing-resistant bootstrap-loop fix this rollout's blocked-user tickets usually turn out to be), `EntraID/Troubleshooting/SSPR-A.md`/`-B.md` (general SSPR troubleshooting — this topic only covers the Sept 7, 2026 explicit-registration tightening specifically) |
| Conditional Access Custom Controls Retirement (confirmed via `WebSearch` against the Microsoft 365 Message Center announcement MC1422061 and multiple independent 2026 migration-guidance sources) — a narrower, CA-specific September 2026 change: no new/modified Custom Controls (the legacy third-party-MFA-redirect grant mechanism) can be created past this date, existing ones keep functioning operationally until full retirement in May 2027, and the GA replacement is External Authentication Methods (External MFA); affects only tenants that configured a Custom Control for a provider like Duo or RSA — no impact otherwise; Mode A deep dive (added run 189) covers the retirement mechanics/rationale, the two-cutoff operational trap (creation freeze is the real deadline, not functional retirement), Graph's structural inability to resolve which provider a Custom Control points to, and the full parallel-run migration playbook | `Security/ConditionalAccess/` (`CustomControlsRetirement-A.md`/`-B.md`, `Scripts/Get-CACustomControlsMigrationAudit.ps1`) | `EntraID/Troubleshooting/ExternalMFA-A.md`/`-B.md` (the replacement feature's own configuration/troubleshooting — this topic covers only the retirement/migration trigger, not day-2 External MFA operation) |
| Microsoft Entra Global Secure Access MCP Firewall (Preview; confirmed via `WebSearch` against the September 2026 "What's new in Microsoft Entra" Tech Community post and a live Microsoft Learn fetch of "Configure Global Secure Access MCP firewall to secure MCP traffic", `ms.date` 2026-08-06) — the network-layer, identity-aware control extending Internet Access SSE to inspect and enforce Allow/Block policy on Model Context Protocol traffic between AI agents and remote MCP servers; the hard TLS-inspection-as-prerequisite dependency (MCP rides inside HTTPS, structurally unparseable without decryption — no error surfaces anywhere else); the three-object configuration chain (MCP filtering policy -> filtering profile/"Security profile" -> Conditional Access session control) where a break at any link enforces on zero traffic; server/primitive (Tool/Resource/Prompt)-level rule scoping via four matching strategies (discovered servers, known servers, discovered tools, manual entry) with first-match-wins priority evaluation; Generative AI Insights as the discover-before-author workflow; and documented Preview-era scope exclusions (local/stdio MCP servers, JSON-RPC batches) — architecturally distinct from Entra Agent ID (agent identity) and M365 Admin Agent/Copilot Agent Governance (publish/approval governance), layered directly on top of and inheriting the fault domain of the base GSA client/tunnel | `EntraID/` (`Troubleshooting/MCPFirewall-A.md`/`-B.md`, `Scripts/Get-MCPFirewallPolicyAudit.ps1`) | `EntraID/Troubleshooting/GlobalSecureAccess-A.md`/`-B.md` (base GSA client/tunnel/forwarding-profile layer this feature depends on entirely — troubleshoot that first if the base tunnel itself is unhealthy), `EntraID/Troubleshooting/AgentID-A.md`/`-B.md` (agent identity, not agent network traffic), `M365/Copilot/AgentGovernance-A.md`/`-B.md` (publish/approval governance, a different control plane) |
| Agents in SharePoint (confirmed via `WebSearch` against Microsoft Learn/Support pages including "Manage access to agents in SharePoint," "Get started with agents in SharePoint," "Create an agent in SharePoint," "Get started with Knowledge Agent," and "Manage trial agents in SharePoint by using PowerShell," identified as a genuine repo gap distinct from the existing SharePoint Advanced Management and M365 Admin Agent/Agent Governance topics via a targeted grep sweep this run) — the three-agent-surface architecture (auto-provisioned ready-made agent per site/library scoped to the site + associated hubs; user-built custom agents hard-capped at 20 sources; the separately-governed content-hygiene Knowledge Agent using its own `KnowledgeAgentScope` tenant setting); the two independent licensing paths (M365 Copilot licence vs. pay-as-you-go metered billing, which superseded the Jan-Jun 2025 free promo and is admin-center-billing-only, not toggled by `Set-SPOCopilotPromoOptInStatus`); the easily-missed side effect that Restricted Content Discovery (RCD) suppresses the Agent icon and all agent features on a site, not just tenant-wide search visibility; the Copilot Control System (formerly Integrated Apps) as the org-wide agent-sharing and usage-oversight surface, which governs sharing scope only and is non-retroactive; and the exclusion-list-based (not allow-list-based) `KnowledgeAgentExcludedSiteIds` mechanism, which silently erodes an intended allow-list as new sites are created | `M365/SharePoint-OneDrive/` (`Agents-A.md`/`-B.md`, `Scripts/Get-SharePointAgentsAudit.ps1`) | `M365/SharePoint-OneDrive/Advanced-Management-A.md`/`-B.md` (Restricted Content Discovery's own primary troubleshooting — this topic covers only its agent-suppression side effect), `M365/AdminAgent/AdminAgent-A.md`/`-B.md` (the distinct M365 Admin Agent for tenant administration, not a content agent), `M365/Copilot/AgentGovernance-A.md`/`-B.md` (cross-agent governance policy in the Copilot Control System, of which SharePoint agent sharing scope is one surface) |
| Microsoft Entra Tenant Governance (GA; confirmed via `WebSearch` against the September 2026 "What's new in Microsoft Entra" Tech Community post and "Microsoft Entra Tenant Governance is now generally available," cross-checked against live Microsoft Learn fetches of the overview, governance-relationships, configuration-management, enable-tenant-discovery, FAQ, and Graph beta `governanceRelationships` reference pages) — a genuine repo gap distinct from both GDAP and ordinary Cross-Tenant Access Settings: native multi-tenant discovery (signal-based **related tenants** — B2B/multitenant-app/billing signals, explicitly NOT implied ownership, `isRelatedTenantsEnabled` a one-way/irreversible tenant setting) plus **governance relationships** (directional governing/governed trust, three-step invitation->request-with-policy-template->accept handshake, one-to-many/many-to-one supported, multi-tier explicitly rejected by the platform except for the additive secure-tenant-creation exception) enabling cross-tenant delegated administration via a security group + optional PIM for Groups activation (governed-tenant PIM policy has no effect on the governing admin — an easy-to-get-backwards asymmetry); **configuration management** built on the already-GA Microsoft Graph Tenant Configuration Management APIs (JSON baselines against 200+ resource types spanning Entra/Intune/Exchange/Teams/Defender/Purview, 6-hour fixed monitor cadence, 200-resource-instance-per-baseline/800-per-tenant-per-day quotas, 7-day snapshot retention); and **secure tenant creation** auto-governing new add-on tenants from birth via a Microsoft Entra ID Free billing asset; explicitly documented to support the MSP/CSP/MSSP multi-customer-tenant scenario, but with a platform-enforced **hard mutual exclusion with Partner Center GDAP** relationships between the same tenant pair — the single highest-value fact for any MSP evaluating this feature, since the two systems cannot be run side-by-side against the same customer | `EntraID/` (`Troubleshooting/TenantGovernance-A.md`/`-B.md`, `Scripts/Get-TenantGovernanceAudit.ps1`) | `EntraID/Troubleshooting/GDAP-A.md`/`-B.md` (Partner Center GDAP — the mutually-exclusive alternative for the same CSP/MSP multi-tenant-administration scenario; a migration between the two is a deliberate cutover, not an additive rollout), `EntraID/Troubleshooting/CrossTenant-A.md`/`-B.md` (ordinary B2B/XTAS end-user collaboration settings — a functionally separate concern from tenant-level governance), `EntraID/Troubleshooting/AccessReviews-A.md`/`-B.md` (single-tenant recertification — distinct from cross-tenant configuration drift monitoring) |
| memberOf Rule Operator Retirement (MC1448379, announced 5 Aug 2026, preview ending **November 3, 2026**; confirmed via `WebSearch` against the Tech Community MC1448379 discussion and office365itpros coverage, cross-checked against live Microsoft Learn fetches of the memberOf configuration page, the dynamic-rule efficiency guide, the administrative-units-dynamic-membership page, and the entitlement-management auto-assignment-policy page including its own Graph PowerShell discovery script) — a genuine, time-boxed repo gap with a hard calendar deadline: three independent object surfaces can use the `memberOf` operator and **fail differently** once retired — dynamic membership **groups** and dynamic **administrative units** silently **freeze** at last-known membership (with `MembershipRuleProcessingState` misleadingly still reporting `On`, giving no signal the freeze occurred), while entitlement management automatic **assignment policies** are instead **quarantined** (assignment processing halts entirely — no additions AND no removals, a materially different mechanism than a frozen snapshot); none of the three produce any visible error or portal warning; the retirement's stated root cause is that a single `memberOf` rule could slow dynamic-group processing tenant-wide, not just for the group using it, which is why there's no simple scale-limit fix; no native feature-parity replacement operator exists as of this writing, so the only current migration paths are a supported-attribute-operator rewrite, conversion to assigned/manual membership, or external/scripted automation | `EntraID/` (`Troubleshooting/MemberOfRetirement-A.md`/`-B.md`, `Scripts/Get-MemberOfRuleAudit.ps1`) | `EntraID/Troubleshooting/DynamicGroups-A.md`/`-B.md` (the general dynamic-group rule engine this operator was one syntax option within — ordinary rule/processing troubleshooting unrelated to memberOf stays there), `EntraID/Troubleshooting/AdministrativeUnits-A.md`/`-B.md` (regular AU mechanics — this topic covers only the memberOf-specific freeze consequence, not AU delegation in general), `EntraID/Troubleshooting/AccessPackages-A.md`/`-B.md` (entitlement management delivery mechanics the quarantined auto-assignment policies sit on top of) |
| Security Administrator Role — Identity Response Expansion (Microsoft change announcement in the September 2026 "What's New in Microsoft Entra" Tech Community post, rollout completing **end of September 2026**, tenant-by-tenant with no admin opt-in/opt-out; confirmed via live fetch of the source blog post and the Microsoft Entra built-in-roles permissions-reference page) — the built-in **Security Administrator** role (`templateId 194ae4cb-b126-40b2-bd5b-6091b380977d`) gains direct identity-response actions against **non-privileged users only**: disable/enable user account, revoke active sign-in sessions, force password reset — the identical action verbs Authentication Administrator/User Administrator already hold, now duplicated onto a role many MSPs assign broadly for its read/reporting purpose, creating a redundant-access hygiene question rather than a privilege-escalation risk (the existing "who can perform sensitive actions" protection model continues to shield privileged-role-holding targets); two independent gates determine actual effect — Active-vs-PIM-Eligible-and-activated assignment state, and target-protection status; Microsoft has not published the literal appended action-string list for this role specifically, so both runbooks and the script verify live against each tenant's actual role definition rather than assuming presence; explicitly distinguished from the separately-announced, architecturally distinct Security Operator/Microsoft-Defender-unified-RBAC SOC-analyst containment-action extension (different portal, different role, different RBAC surface) | `EntraID/` (`Troubleshooting/SecurityAdminRoleExpansion-A.md`/`-B.md`, `Scripts/Get-SecurityAdminRoleAudit.ps1`) | `EntraID/Troubleshooting/PIM-A.md`/`-B.md` (base PIM activation/approval workflow this topic only applies as a gating factor, not a full PIM how-to), `EntraID/Troubleshooting/IdentityProtection-A.md`/`-B.md` (standard identity-incident investigation procedure once misuse via these new actions is suspected — this topic covers the permission model, not incident response itself) |
| SOC Identity Responder Role (new built-in Microsoft Entra role, introduced ~June-July 2026; confirmed via live fetch of Microsoft's own `privileged-roles-permissions` reference, which names it twice alongside **Security Operator** with identical non-privileged-target-only restriction language — a secondary community source, Topedia Blog 19 Jul 2026, adds an unconfirmed role template ID `58f930cc-fcf4-4152-852c-1d7dbf502139` and a four-action permission screenshot) — a **separate, purpose-built** containment role (disable/enable user, revoke sessions via `invalidateAllRefreshTokens`, reset password — the identical four action strings as the Security Administrator expansion above, but via a structurally distinct role) that Microsoft originally described in a June 2026 announcement as an extension of the pre-existing **Security Operator** role before ultimately shipping it as its own role instead — decouples read/investigate access (Security Operator) from write/containment power (SOC Identity Responder) as a deliberate least-privilege boundary; dual assignment surface (classic Entra Roles blade, or the Microsoft-recommended Defender portal Permissions -> Microsoft Defender XDR -> Roles / unified RBAC path, which requires the assignor to hold at least Security Administrator and scopes the assignee's visible UI to the Defender portal only) with optional Administrative Unit scoping as the primary blast-radius control; hard "can't perform actions on privileged accounts" restriction with no documented exception path for this role (unlike Privileged Authentication Administrator/Global Administrator); two additional actions referenced in Microsoft's broader announcement language ("mark user compromised", "delete individual authentication method") are explicitly NOT confirmed present in the base role definition — both runbooks and the script verify live per tenant rather than assuming either state or trusting the community-reported template ID | `EntraID/` (`Troubleshooting/SOCIdentityResponder-A.md`/`-B.md`, `Scripts/Get-SOCIdentityResponderAudit.ps1`) | `EntraID/Troubleshooting/SecurityAdminRoleExpansion-A.md`/`-B.md` (the separate, broader Security Administrator role gaining the identical four action verbs through a different Microsoft announcement — the two produce structurally identical audit-log entries and require assignee-list correlation, not action-name inspection, to disambiguate), `EntraID/Troubleshooting/PIM-A.md`/`-B.md` (base PIM activation/approval workflow this topic only applies as a gating factor), `EntraID/Troubleshooting/AdministrativeUnits-A.md`/`-B.md` (the general AU-scoped role-assignment mechanism this role's optional scoping control builds on) |
| AD Group Enforcement in Microsoft Entra Cloud Sync (Preview; confirmed via the September 2026 "What's new in Microsoft Entra" Tech Community post and live Microsoft Learn fetches of the group-provisioning tutorial and the dedicated how-to-ad-group-enforcement page) — a DC-side governance layer on top of the existing Group Provisioning to AD DS feature (`CloudSync-A.md`/`-B.md`): a new `CN=SOA-Policies`/`CN=CloudSyncSOAPolicy` policy object that blocks on-premises LDAP modify/Recycle-Bin-restore operations against any group marked `msDS-ObjectSoa = Cloud`, for any SID not present in a `msDS-Settings` break-glass allow-list — additive to, never a replacement for, existing AD RBAC; enablement is per-domain-controller via a two-artifact Known-Issue-Rollback model (a dormant cumulative-update code path plus a separate Group Policy MSI switch, minimum `ntdsai.dll` 10.0.20348.5257 for Server 2022 / 10.0.26100.32995 for Server 2025), meaning a partial rollout across the DC fleet is a real, not partial, protection gap; Enforced (hard block) vs. Audit (log-only, requires Security Diagnostics registry enablement) modes set via the `Set-CloudSyncSOAPolicy.ps1`/`Check-CloudSyncSOAPolicy.ps1` scripts from the AzureAD/EntraIDGovernance GitHub repo — no Entra admin center UI surface exists for this preview; the counterintuitive empty-allow-list behavior (zero SIDs in `msDS-Settings` means the policy is effectively OFF, not maximally locked down); and documented current-preview limitations carried into both runbooks verbatim rather than inferred — groups-only (no user-object support, since user provisioning to AD via the agent doesn't exist as a feature yet), and no block on outright deletion (only modify/restore) | `EntraID/` (`Troubleshooting/ADGroupEnforcement-A.md`/`-B.md`, `Scripts/Get-ADGroupEnforcementStatus.ps1`) | `EntraID/Troubleshooting/CloudSync-A.md`/`-B.md` (the underlying Group Provisioning to AD DS feature this governance layer requires and builds on top of — this topic assumes GPAD is already configured and working), `EntraID/Troubleshooting/GroupWritebackMigration-A.md`/`-B.md` and `CloudSyncMigration-A.md`/`-B.md` (adjacent Cloud Sync migration topics in the same folder, architecturally unrelated to this governance-layer feature) |
| Intune Device Action Daily Quotas (Wipe/Retire/Delete) — confirmed via live Microsoft Learn fetch of the Wipe (`ms.date` 2026-08-21), Retire, and Delete (both `updated_at` 2026-08-18) action pages — the three independent tenant-wide daily submission caps (500 Wipe/1,000 Retire/1,000 Delete), cumulative across single-device, bulk, and Graph API submissions with no separate automation allowance; Delete's cascade into an underlying Retire (Windows/Apple mobile/macOS/Android BYOD-work-profile/device-administrator) or Wipe (Android corporate-owned COBO/COSU/COPE/AOSP) command, consuming BOTH quotas simultaneously for the Android corporate-owned case, per Microsoft's explicit statement that the Delete limit applies "even when deleting a device triggers a Retire or Wipe command"; and the architecturally separate BitLocker key-protector-removal safeguard (no cap, always fires) on Delete/Retire of an Entra-joined, BitLocker-protected device | `Intune/` (`Troubleshooting/DeviceActionQuotas-A.md`/`-B.md`, `Scripts/Get-DeviceActionQuotaAudit.ps1`) | `Intune/Troubleshooting/MultiAdminApproval-A.md`/`-B.md` (an independent execution gate — pending-approval actions don't consume quota until approved — not to be conflated with a quota block) |
| Intune macOS 15 Minimum Version Requirement (Golden Gate 27) — Plan for Change (Message Center MC1403402, published 2026-06-24; no confirmed cutover date as of this writing) — Intune/Company Portal/Intune MDM agent for macOS moving their minimum supported version to macOS 15 (Sequoia)+ shortly after Apple's expected "Golden Gate 27" release later in 2026, timed to follow Apple's release specifically because Company Portal is a single unified iOS+macOS app codebase; the two-effect model (already-enrolled macOS 14.x-or-below devices are NOT retroactively affected; only NEW enrollment attempts are blocked below the floor); and the separately-documented, not-identical support nuance for devices enrolled via ADE without user affinity or Direct Enrollment (shared/kiosk Macs), referenced at `aka.ms/Intune/macOS/ADE-DE-support` rather than restated in the general announcement | `macOS/` (`Troubleshooting/macOS15MinimumVersion-A.md`/`-B.md`, `Scripts/Get-MacOS15ReadinessAudit.ps1`) | `macOS/Troubleshooting/SoftwareUpdates-A.md`/`-B.md` (the mechanism for pushing the actual OS upgrade at scale via Intune, once devices are identified), `macOS/Troubleshooting/ADE-Enrollment-A.md`/`-B.md` (general ADE enrollment mechanics this version floor gates) |
| Legacy macOS Update Policy Retirement (MDM → DDM migration) — Apple's deprecation of MDM-based software-update commands is retiring Intune's legacy "Update policies for macOS" console feature (`macOSSoftwareUpdateConfiguration`); no confirmed retirement date published as of this writing, but Microsoft's own guidance already says don't use it on macOS 13+ (DDM Software Update Enforcement is the sole recommended mechanism there) while macOS 12 and older still depend on it as the only option; no 1:1 field mapping between the legacy per-update-type behavior/schedule model and DDM's target-version-and-deadline model — a redesign, not a lift-and-shift; safe migration sequencing is add-DDM-profile → validate on a pilot group → remove legacy assignment, not a simultaneous swap | `macOS/` (`Troubleshooting/LegacyUpdatePolicyRetirement-A.md`/`-B.md`, `Scripts/Get-LegacyUpdatePolicyMigrationAudit.ps1`) | `macOS/Troubleshooting/SoftwareUpdates-A.md`/`-B.md` (DDM architecture and enforcement troubleshooting once migrated — this topic is scoped to the migration decision/sequencing only), `macOS/Troubleshooting/macOS15MinimumVersion-A.md`/`-B.md` (a separate Plan-for-Change topic with the same no-confirmed-date pattern) |
| Microsoft Purview Network Data Security (preview) — the Entra Global Secure Access Internet Access content-policy integration extending Purview's DLP classification engine to network traffic bound for unmanaged cloud apps and generative AI (ChatGPT, Gemini, Claude, Dropbox, Gmail, and 35,000+ Defender-for-Cloud-Apps-catalog-backed destinations); two required, independently-configured policy objects (Entra-side content policy choosing Basic Allow/Block-by-MIME-type vs. Scan with Purview — the latter the only path that ever inspects text, never Basic; Purview-side "Inline web traffic" DLP policy providing the actual audit/block decision) with neither sufficient alone; request-based Purview pay-as-you-go billing as a hard prerequisite gate for selecting Network as an enforcement location (though GSA-path usage itself is free during preview); exact-FQDN destination matching with no top/second-level wildcard support as the leading false-negative cause, since AI-app upload traffic routes through non-obvious dedicated subdomains; documented current-preview limits (no B2B guest coverage, no UDP/QUIC/HTTP-3, ZIP-only compressed-content detection) | `Security/Purview/` (`NetworkDataSecurity-A.md`/`-B.md`, `Scripts/Get-NetworkDataSecurityReadiness.ps1`) | `Security/Purview/DLP-Policy-A.md`/`-B.md` (the mature Exchange/SharePoint/OneDrive/Teams/Endpoint DLP workloads this topic's classification engine is shared with, but a different enforcement plane), `EntraID/Troubleshooting/GlobalSecureAccess-A.md`/`-B.md` (the GSA tenant/identity-side layer this topic's content policy is configured within) |
| Windows Autopatch Cloud-Based Quality Update Policies (rollout Sept 1 – Oct 15 2026, confirmed via live Microsoft Learn fetch dated 2026-09-01) — a new admin-facing approval/deferral/pause control object layered over, not replacing, Autopatch's existing ring orchestration and automatic incident-triggered pause; four independently-configured release categories (monthly security — default Automatic; monthly non-security preview, OOB security, OOB non-security — all default Manual) governing both Windows OS quality updates and, for Windows 11 devices only, the matching .NET Framework update category in the same policy object; 0-30 day deferral for Automatic categories with manual-override-early support; Pause revokes approval for new devices only and never rolls back already-installed devices, with up to 8h Intune check-in latency for pause/resume to reach devices; approval method immutable once a policy is created (new policy + reassignment required to change it); documented precedence (cloud-based policy wins approval/deferral, legacy Update ring policy's deadline/grace-period settings remain active) when both target one device; and a documented hotpatch conflict where approving non-security updates on a hotpatch-enabled policy silently removes the device from its hotpatch cycle for that release | `Intune/` (`Troubleshooting/QualityUpdatePolicies-A.md`/`-B.md`, `Scripts/Get-QualityUpdatePolicyAudit.ps1`) | `Intune/Troubleshooting/Autopatch-A.md`/`-B.md` (the ring/deployment orchestration and automatic-pause layer this new policy object is layered over, not a replacement for), `Intune/Troubleshooting/Hotpatch-A.md`/`-B.md` (the documented conflict between non-security-update approval and hotpatch enrollment) |

---

## Cross-Domain Rules

Before answering any identity-related question, check `EntraID/` — almost everything touches identity.

Before answering any device management question, check `Intune/` — policies flow from there.

Before answering any security/access question, check `Security/ConditionalAccess/` — CA overrides most other access logic.

If PowerShell is needed for a fix, look for an existing script in the relevant `Scripts/` folder first.

---

## Technology Ranking by MSP Frequency

Built from real-world MSP ticket patterns. Build order = priority order.

| Rank | Domain | Why it's here |
|------|--------|---------------|
| 1 | DFS | SMB/enterprise staple; replication failures = user impact immediately |
| 2 | Power Automate | SharePoint automation is in every org; permission flows break constantly |
| 3 | Intune | Every managed device touches this; policy conflicts are daily |
| 4 | Windows Update / WfUB | Perpetual pain; WSUS conflicts, update rings, dual-scan |
| 5 | Entra ID + Hybrid Join | Identity is the dependency for everything |
| 6 | Conditional Access | Policies break access silently; hardest to diagnose under pressure |
| 7 | Autopilot | Enrollment failures cost hours; TPM, hash, network all in play |
| 8 | Exchange Online | Mail flow rules, hybrid coexistence, shared mailboxes |
| 9 | BitLocker | Recovery key gaps, escrow failures discovered at worst time |
| 10 | SharePoint/OneDrive | Sync client issues, permission inheritance breaks, migration |
| 11 | Defender for Endpoint | ASR rules blocking legit apps, onboarding gaps |
| 12 | Teams | Calling plans, device policies, federation |
| 13 | Azure AD Connect / Entra Connect | Sync errors, attribute conflicts, password hash |
| 14 | M365 Licensing | Group-based licensing failures, service plan conflicts |
| 15 | macOS via Intune | ADE enrollment, profile delivery, shell script failures |
| 16 | Active Directory (on-prem AD DS) | Every other domain here depends on it; replication/FSMO failures cascade into DFS, hybrid join, Kerberos auth |

---

## Repo Conventions

- `_AGENT.md` in every folder — agent-specific instructions for that domain
- `Troubleshooting/Topic/Topic-B.md` — Hotfix runbook
- `Troubleshooting/Topic/Topic-A.md` — Deep Dive reference
- `Scripts/` — PowerShell scripts, named with `Verb-Noun.ps1` pattern
- All scripts follow: Preflight → Detect → Execute → Validate → Report

---

## Learning Pointers Trigger

After every resolved issue, append this section to your response:

```
## 🎓 Learning Pointers
Based on what just happened, here are things worth exploring:
- [concept] — why it matters here
- [tool/cmdlet] — what it does and when you'd reach for it
- [community resource] — thread/doc that goes deep on this edge case
```
